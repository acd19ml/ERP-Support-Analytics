先把最要紧的三条判断放前面,后面再展开路径。

## 三个需要先纠正的认知

**1. 你手上不是 200 条问答对,是 200 条"结案记录"。**
写解决方案的专员,掌握的信息严格多于模型能看到的输入(标题+描述)。电话补全的那几轮就是信息差。这意味着任何以"标题 → 解决方案链接"做的离线评估,都会系统性高估线上表现,而且高估的幅度你现在不知道。这个偏差必须先量化,否则后面所有数字都不可信。

**2. 动手第一件事不是建 wiki,是统计链接基数。**
200 条问题最终指向多少个不同的 SOP 链接?如果是 15~30 个,这就是一个小类目的**分类/路由问题**,不是生成问题,方案复杂度可以降一个数量级——BM25 或者嵌入检索 + 阈值就能吃掉大半。如果是 100+ 且长尾严重,说明 SOP 粒度太碎,先要合并整理内容,而不是套模型。这个统计半天能出,但决定后面所有选型。

**3. 你表里已有的"解决办法(功能/数据)"二分,就是可答性上界。**
"数据"类问题通常要后台改配置/补数据,LLM 给链接是无效回复。这部分应该在评估集里单独拆开,否则你的准确率天花板被无意义地压低,还会误导你去优化错误的方向。

## 动手前的数据体检(建议 1~2 天)

按这四个维度过一遍 200 条:

- **链接分布**:去重后多少个?每个链接覆盖几条?长尾占比
- **可答性分层**:
 - A 自足 — 仅凭标题唯一确定 SOP
 - B 需澄清 — 缺 1~2 个槽位(哪个应用/哪个角色/申请中还是已通过)
 - C 不可答 — 数据/配置类,必须转人工
 A/B/C 的比例直接决定你要不要做多轮澄清。如果 B 占 40% 以上,单轮"文本+链接"方案的上限就已经封死在 60%。
- **问题阶段分布**:RBAC 下权限问题至少有四个阶段——不知道怎么申请 / 申请被拒 / 已通过但仍无权 / 权限过多要收回。第三类是**排查问题**不是 SOP 问题,最容易被 wiki 方案漏掉,单独统计占比。
- **租户差异**:不同租户的角色命名和审批流是否一致?如果租户可自定义角色名,"申请 XX 权限"在不同租户下映射不同链接,这是个隐藏的硬约束,要尽早问 PM。

## 分层设计(替代"LLM Wiki"这个说法)

我建议不要建一份新的 wiki 副本——那是双份维护,必然漂移。改成三层:

- **内容层**:原 SOP 保持唯一真源,不复制。定期 reindex 即可,更新问题自动消解
- **路由层**:你真正要沉淀的东西。结构是 `问题表象 → 所需槽位 → 目标链接 + 前置条件`。这层是人工可审、可版本化的,也是 PM 能参与维护的
- **对话层**:槽位不全时发起澄清,槽位齐全时给链接,置信度不足时**拒答转人工**

第三层的拒答机制在权限场景是刚需:多租户下告诉用户去申请错误的角色,是越权申请风险,不是简单的答错。所以评估里要单独看"高置信错误率",而不只是看准确率。

## Baseline 怎么定

- **Baseline 0**:BM25 / 关键词 → top-1 链接。必须先跑,它是 LLM 增量的参照系。很多这类场景 BM25 就能到 65~75%
- **Baseline 1**:嵌入检索 top-k + LLM 重排选链接
- **指标**:链接 top-1 / top-3 命中率,而非文本相似度——你的交付物是链接
- **数据切分**:200 条别全拿来调 prompt。留至少 60 条 test 完全不看,而且要按链接分层抽样,否则长尾全落在一边

另外补一句:你自己负责的**页面级帮助是被低估的杠杆**。工单里带了子产品/模块/应用字段,这就是页面上下文。同样一个模糊问题,在页面内提问能自动补全 2~3 个槽位,在工单系统提问则全靠追问。如果这个能力最终能嵌入产品内,B 类问题会大量降级成 A 类。反向也成立:工单高频问题正好暴露了页面级帮助的覆盖缺口。

需要我帮你把体检的统计口径或者路由层的 schema 具体展开吗?