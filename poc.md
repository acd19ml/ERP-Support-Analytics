# MetaERP 权限客服 Agent POC 方案

> **文档版本：** V2.0
> **文档状态：** POC 立项与实施方案
> **适用范围：** MetaERP 权限类客服问题
> **目标读者：** 产品、客服、权限平台、业务 Owner、架构、研发、测试、数据及安全团队
> **方案目标：** 用一个范围明确、事实可准备、结果可验证的权限问题切片，验证 Agent 是否能够安全、准确地完成问题理解、必要追问、权限诊断、解决建议和人工转交。

---

## 一、执行摘要

MetaERP 权限类客服问题并不是简单的 FAQ 问答。

当用户表示“页面进不去”“按钮不能用”“查不到某公司的数据”或“不知道申请什么权限”时，人工客服通常需要完成以下工作：

1. 理解用户真正遇到的表象；
2. 定位涉及的系统、页面、功能或业务对象；
3. 判断当前信息是否足够；
4. 必要时追问用户；
5. 查询用户已有角色、数据维度、有效期及当前角色；
6. 确认目标功能需要什么角色和数据范围；
7. 判断缺少的是角色、数据维度，还是根本不是权限问题；
8. 检查申请资格、跨 BG、涉密和其他安全限制；
9. 给出精确的申请、切换、续期或操作建议；
10. 无法可靠判断时转人工处理。

因此，本项目不能被定义为：

```text
历史工单
→ 相似工单检索
→ 返回历史解决方案
```

也不能被定义为：

```text
把工单总结成一张四列表
→ 直接作为 Agent 知识库
```

本 POC 要验证的是：

> 在一个明确的高频权限问题切片内，Agent 能否利用问题模式、权威权限知识、用户实时权限状态和确定性校验规则，安全地完成一部分客服诊断；对于无法确认的问题，能否正确追问或转人工。

POC 不以覆盖全部权限问题为目标，也不预先建设完整权限知识图谱。第一阶段只选择：

```text
一个子产品
+ 一个确定的查询功能或业务对象
+ 一种主要数据维度
+ 四类可确定诊断的权限原因
+ 明确的非权限及复杂问题转人工路径
```

POC 第一阶段只提供建议，不自动授予、修改或申请权限。

---

# 二、项目定义

## 2.1 项目要解决的问题

本项目需要回答五个核心问题。

### 问题一：Agent 如何听懂用户

用户通常不会提供资源点 ID、功能点 ID 或标准角色名称。

用户可能只会说：

* 我看不到广州公司的数据；
* 这个页面没有权限；
* 为什么别人能看我不能看；
* XX 报表搜不到数据；
* 我的权限是不是过期了；
* 这个按钮点不了；
* 我应该申请哪个角色。

Agent 需要通过术语、用户表达、页面名称、URL、报错、截图和对话上下文，将用户语言映射到系统中的产品、功能、业务对象、角色和数据维度。

### 问题二：Agent 如何判断还缺什么信息

不同问题需要的上下文不同。

有的问题需要页面或 URL，有的问题只需要角色名称，有的问题需要目标公司，有的问题需要报错原文。

Agent 不能固定询问所有信息，也不能在上下文不足时直接猜测。

### 问题三：Agent 如何判断问题原因

同样是“查不到数据”，可能是：

* 缺少功能角色；
* 已有角色但缺少数据维度；
* 角色或维度权限已经过期；
* 当前激活了错误角色；
* 查询条件不正确；
* 数据尚未生成；
* 跨 BG 权限不互通；
* 涉及涉密或特殊权限；
* 后台配置异常；
* 系统缺陷；
* 当前知识与工具无法覆盖。

Agent 必须逐步排除候选原因，而不是直接返回最高频的历史措施。

### 问题四：Agent 如何安全地给出动作

即使确认用户缺少某个角色，也不代表用户一定有资格申请。

Agent 还需要确认：

* 角色是否需要申请；
* 是否为自动激活角色；
* 是否存在岗位或部门限制；
* 是否涉及跨 BG、涉密或高风险权限；
* 是否已有等价角色；
* 需要补角色还是补数据维度；
* 申请入口和生效时间；
* 生效后如何验证。

### 问题五：什么时候必须转人工

Agent 必须能够明确识别：

* 功能无法唯一定位；
* 权限系统数据不可用；
* 权威规则缺失或冲突；
* 用户状态与系统现象矛盾；
* 涉及跨 BG、涉密或高风险权限；
* 疑似业务数据异常；
* 疑似后台配置或系统缺陷；
* 超过最大追问轮数；
* 当前结论无法被证据支持。

---

# 三、POC 目标

## 3.1 业务目标

POC 需要验证以下业务价值：

1. 一部分高频权限问题能否不进入人工客服；
2. Agent 是否能够减少用户重复申请已有权限；
3. Agent 是否能够区分缺角色和缺数据维度；
4. Agent 转人工时，是否能携带完整诊断信息，减少人工重复追问；
5. 用户获得解决建议后的平均处理时间是否下降；
6. Agent 是否会产生错误权限申请或越权风险。

## 3.2 技术目标

POC 需要验证以下技术能力：

1. 在线问题理解与现有离线分类是否可以分离；
2. 问题模式 Schema 是否能够驱动逐步诊断；
3. Agent 是否能够选择最少必要追问；
4. 是否能够可靠定位目标功能或业务对象；
5. 用户权限查询能力是否足以支持诊断；
6. 小范围角色及权限规则是否能够形成权威访问策略；
7. 确定性规则能否完成角色、维度、有效期等差异判断；
8. LLM 是否能够基于确定性诊断结果生成可理解的客服回答；
9. 工具失败、知识缺失和来源冲突时是否能安全降级；
10. 运行日志是否能够完整还原每次判断依据。

## 3.3 POC 不验证的内容

第一阶段不验证：

* 全部权限专题问题；
* 全部产品和子产品；
* 完整岗位—角色关系；
* 企业级权限知识图谱；
* 全自动权限申请或授权；
* 跨 BG 权限自动处理；
* 涉密权限自动处理；
* 高风险角色自动推荐；
* 业务数据异常自动诊断；
* 后台配置或缺陷自动修复；
* Agent 完全替代人工客服；
* 某一种知识存储技术的最终选型。

---

# 四、POC 范围

## 4.1 POC 最小决策单元

POC 不直接以“权限专题”或“L1=7 查不到数据”作为实施范围。

第一阶段范围定义为：

```text
一个子产品
×
一个明确查询功能、页面或业务对象
×
一种主要数据维度
×
一个用户访问场景
```

统一将该范围称为：

```text
POC-QueryScope
```

其标准问题表象为：

> 用户已经进入指定查询功能，但查不到目标公司、组织或其他目标维度下的数据。

具体子产品和功能必须在 POC 启动阶段通过数据筛选确定，并在开发开始前冻结。

## 4.2 POC 切片选择标准

候选功能必须同时满足：

1. 最近一段时间内工单量较高；
2. 工单中存在多个已确认解决案例；
3. 用户问题表象相对集中；
4. 功能—角色—数据维度关系能够由系统或 Owner 确认；
5. 能够获取用户当前角色、有效期和数据维度；
6. 不属于涉密或高风险权限；
7. 不需要跨多个权限系统才能判断；
8. 解决后能够由用户或系统验证；
9. 业务 Owner 和角色 Owner 能够参与 POC；
10. 权限规则在 POC 期间相对稳定。

若多个候选功能均满足条件，优先选择：

```text
工单量高
×
权威数据完整
×
诊断结果可验证
×
安全风险低
```

而不是单纯选择工单量最高的功能。

## 4.3 POC 自动诊断原因

第一阶段自动诊断只覆盖以下原因：

| 原因代码                    | 含义                  | 自动处理方式         |
| ----------------------- | ------------------- | -------------- |
| `MISSING_ROLE`          | 用户缺少访问该功能所需角色       | 推荐精确角色和申请入口    |
| `MISSING_DATA_SCOPE`    | 用户已有角色，但缺少目标数据维度    | 推荐需要补充的角色实例及维度 |
| `EXPIRED_PERMISSION`    | 角色或数据权限已失效          | 提供续期或重新申请指引    |
| `WRONG_ACTIVE_ROLE`     | 用户已有所需权限，但当前角色上下文错误 | 指导切换到正确角色      |
| `QUERY_CONDITION_ISSUE` | 权限满足，但查询条件遗漏目标范围    | 提供经审核的操作指引     |

其中，`QUERY_CONDITION_ISSUE` 只有在查询规则清晰且已由业务 Owner 核实时，才进入自动处理范围。

## 4.4 只识别、不自动解决的原因

以下原因允许 Agent 识别，但必须转人工：

| 原因代码                          | 处理方式                 |
| ----------------------------- | -------------------- |
| `CROSS_BG_RESTRICTION`        | 说明可能涉及跨 BG 权限，转人工    |
| `CONFIDENTIAL_SCOPE`          | 涉密或特殊范围，立即转人工        |
| `BUSINESS_DATA_NOT_CREATED`   | 疑似业务数据尚未产生，转业务客服     |
| `DATA_SYNC_DELAY`             | 疑似同步问题，转对应系统团队       |
| `BACKEND_CONFIGURATION_ISSUE` | 疑似后台配置异常，转运维         |
| `SYSTEM_DEFECT`               | 疑似系统缺陷或性能问题，转研发或缺陷流程 |
| `POLICY_CONFLICT`             | 权威规则之间存在冲突，转 Owner   |
| `UNKNOWN`                     | 无法形成有证据支持的判断，转人工     |

## 4.5 动作边界

第一阶段 Agent 允许：

* 查询用户权限；
* 查询选定功能的访问策略；
* 提出追问；
* 解释诊断结果；
* 推荐申请角色；
* 推荐补充数据维度；
* 提供切换、续期和查询指引；
* 生成转人工摘要。

第一阶段 Agent 不允许：

* 自动提交权限申请；
* 自动审批；
* 自动修改用户权限；
* 自动配置角色或数据维度；
* 推荐未核实角色；
* 根据工单推测岗位申请资格；
* 对高风险、涉密或跨 BG 权限给出确定申请建议。

---

# 五、POC 核心假设

POC 需要通过实验验证以下假设，而不是默认这些假设成立。

## H1：结构化问题模式优于相似工单回答

当 Agent 使用：

```text
表象
→ 必要上下文
→ 候选原因
→ 检查条件
→ 动作
```

结构时，其诊断准确率和追问有效性应高于直接检索相似工单并复述解决方案。

## H2：实时权限状态能够显著减少错误建议

接入用户角色、维度、有效期和当前角色后，Agent 应能减少：

* 重复申请已有角色；
* 将缺维度误判为缺角色；
* 忽略权限过期；
* 忽略当前角色切换问题。

## H3：安全关键计算应由确定性规则完成

角色是否存在、维度是否覆盖、权限是否过期等判断，由后端规则或确定性工具完成时，应比 LLM 自行比较权限对象更稳定、更可审计。

## H4：明确的 UNKNOWN 和转人工机制能够控制风险

当知识、数据或工具不足时，Agent 应停止猜测。引入 `UNKNOWN` 和强制转人工条件后，不安全建议率应显著降低。

## H5：结构化转人工能够降低人工处理成本

即使 Agent 无法闭环，也应将以下信息一并转交：

* 用户原始诉求；
* 已识别功能；
* 已补充上下文；
* 查询过的用户权限；
* 已排除原因；
* 当前候选原因；
* 缺失的权威信息；
* 建议接手团队。

人工客服不应重新从零开始询问。

---

# 六、实验设计

## 6.1 对比方案

POC 离线评测采用同一批盲测案例，对比三种方案。

### 方案 A：相似工单基线

输入用户问题，检索相似历史工单并生成回答。

不接用户权限 API，不使用结构化问题模式。

该方案代表最轻量的 RAG 或 FAQ 路径。

### 方案 B：问题模式但无实时权限

使用结构化问题模式和角色知识，但不查询用户实时权限。

该方案可以提出排查建议，但不能确认用户实际缺少什么。

输出必须保留不确定性，例如：

> 该情况可能与目标公司的数据维度有关，请检查当前角色是否已包含该范围。

### 方案 C：目标 POC 方案

使用：

* 在线问题理解；
* 结构化问题模式；
* 小范围权威访问策略；
* 用户实时权限查询；
* 确定性差异判断；
* 安全规则；
* Agent 回答生成。

方案 C 是最终拟上线方案。

## 6.2 对比目的

通过三组对比回答：

1. 单纯相似工单能够解决多少问题；
2. 问题模式带来的实际提升是多少；
3. 实时权限状态带来的实际提升是多少；
4. 哪些问题即使接入权限 API 仍然无法解决；
5. 是否真的需要进一步建设功能—资源—角色映射；
6. 是否有必要建设权限差异诊断 API；
7. 哪些判断必须由后端承担。

---

# 七、现有分类体系的使用方式

## 7.1 离线分类器

现有 `classify.py` 使用：

```text
标题
+
问题描述
+
历史解决方案
```

输出：

```text
专题
+
L1
+
适用时的 L2
+
历史解决方案 S 分类
+
边界标记
```

它继续用于：

* 历史工单整理；
* POC 候选工单筛选；
* 问题模式候选发现；
* 分布统计；
* 边界案例抽样；
* 离线评测集建设；
* 运行结果回写后的分析。

## 7.2 在线问题理解器

在线用户问题没有历史解决方案，因此必须使用独立的在线判据。

在线问题理解器输入：

```text
用户当前问题
+
当前对话
+
入口系统提供的用户和页面上下文
```

输出：

```json
{
  "topic": "权限专题",
  "l1": "7-查不到数据",
  "l2": null,
  "confidence": 0.88,
  "entities": {},
  "missing_context": [],
  "candidate_patterns": [],
  "ambiguities": [],
  "routing_action": "ENTER_PERMISSION_DIAGNOSIS"
}
```

在线入口阶段不得输出最终 S 分类。

S 分类只在诊断结束后记录本次实际解决结果，例如：

```text
最终动作为申请角色
→ S1

最终动作为补充数据维度
→ S2

最终动作为切换角色或解释权限规则
→ S4c

最终转人工
→ S7
```

## 7.3 用户提到权限不代表原因一定是权限

用户可能说：

> 是不是我没权限，所以看不到数据？

其中“没权限”只是用户猜测。

在线理解器必须分别输出：

```json
{
  "observed_symptom": "查不到目标数据",
  "user_hypothesis": "可能缺权限"
}
```

不能直接把用户猜测作为诊断结果。

---

# 八、运行时决策流程

## 8.1 总体流程

```text
用户提出问题
      ↓
专题路由
      ↓
问题表象与实体识别
      ↓
POC 范围判断
      ↓
上下文完整性检查
      ↓
必要追问
      ↓
功能或业务对象定位
      ↓
加载对应问题模式
      ↓
查询用户权限状态
      ↓
查询目标功能访问策略
      ↓
确定性权限差异诊断
      ↓
安全与资格校验
      ↓
生成解决建议
      ↓
用户验证
      ↓
闭环 / 再诊断 / 转人工
```

## 8.2 状态机

Agent 对话状态定义如下：

| 状态                     | 含义            |
| ---------------------- | ------------- |
| `RECEIVED`             | 收到用户问题        |
| `ROUTING`              | 判断是否进入权限 POC  |
| `OUT_OF_SCOPE`         | 不在 POC 范围     |
| `COLLECTING_CONTEXT`   | 补充必要上下文       |
| `RESOLVING_FUNCTION`   | 定位功能或业务对象     |
| `LOADING_PATTERN`      | 加载问题模式        |
| `QUERYING_FACTS`       | 查询用户权限和访问策略   |
| `DIAGNOSING`           | 执行确定性检查       |
| `SAFETY_CHECKING`      | 执行资格和风险校验     |
| `RECOMMENDING`         | 输出解决动作        |
| `WAITING_CONFIRMATION` | 等待用户验证结果      |
| `RESOLVED`             | 用户确认已解决       |
| `RETRYING`             | 第一次建议未解决，重新诊断 |
| `ESCALATING`           | 生成转人工信息       |
| `CLOSED`               | 会话结束          |

## 8.3 最大追问原则

Agent 应优先使用系统已有信息，避免重复询问用户。

第一阶段建议：

* 同一个缺失字段最多追问一次；
* 连续追问不超过三轮；
* 无法定位功能时不继续权限推荐；
* 用户无法提供页面或 URL 时，可以使用功能名称、业务对象、报错、截图或产品上下文替代；
* 三轮后仍不能满足最低上下文，转人工。

## 8.4 最少信息增益原则

Agent 每次只问对当前分支最有区分度的问题。

例如当前无法区分“缺角色”和“缺维度”，但功能尚未定位，优先问：

> 您查询的是哪个页面或哪类业务数据？

而不是先问：

> 您有哪些角色？

用户角色应优先通过系统查询，而不是要求用户自行查看。

---

# 九、总体架构

## 9.1 架构组成

```text
用户入口
  ↓
在线专题路由器
  ↓
在线问题理解器
  ↓
对话上下文管理器
  ↓
功能定位器
  ↓
问题模式服务
  ↓
用户权限查询服务
  ↓
访问策略服务
  ↓
确定性权限诊断引擎
  ↓
安全规则引擎
  ↓
回答生成器
  ↓
用户确认与转人工
  ↓
诊断日志与评测平台
```

## 9.2 LLM 的职责

LLM 负责：

* 理解用户自然语言；
* 识别页面、功能、业务对象、角色和维度表达；
* 判断缺失上下文；
* 选择合适追问；
* 在候选问题模式中进行语义匹配；
* 调用工具；
* 根据确定性结果生成客服解释；
* 组织转人工摘要。

## 9.3 后端和规则的职责

后端或确定性规则负责：

* 查询用户真实角色；
* 查询角色状态和有效期；
* 查询数据维度；
* 查询当前活动角色；
* 提供功能访问策略；
* 比较角色是否满足；
* 比较维度是否覆盖；
* 判断权限是否过期；
* 判断访问条件是否满足；
* 执行安全限制；
* 返回明确原因代码；
* 保存审计日志。

## 9.4 禁止交给 LLM 的判断

以下判断不得仅依赖 LLM：

* 用户是否实际拥有某角色；
* 某权限是否已经过期；
* 数据维度是否覆盖目标组织；
* 角色是否满足确定的访问策略；
* 用户是否具备正式申请资格；
* 涉密和高风险权限能否申请；
* 复杂角色组合的布尔计算；
* 权威来源冲突时选择哪一个事实。

---

# 十、知识与数据分层

## 10.1 用户表达与术语知识

解决用户语言和系统语言不一致的问题。

包括：

* 产品和子产品名称；
* 页面、菜单和按钮名称；
* 业务对象；
* URL 特征；
* 角色标准名称；
* 角色别名和缩写；
* 数据维度名称；
* 报错原文；
* 常见用户措辞；
* 易混淆术语。

主要来源：

* 历史工单；
* 页面和菜单配置；
* 角色目录；
* 客服经验；
* 产品文档。

## 10.2 问题模式知识

解决以下问题：

* 这种表象可能有哪些原因；
* 当前还缺哪些信息；
* 应优先查询什么；
* 应提出什么问题；
* 每个结果对应什么动作；
* 什么情况下必须转人工。

问题模式来自历史工单和客服经验，但必须经过人工审核。

## 10.3 权威访问策略

解决以下问题：

* 目标功能需要哪些角色；
* 多个角色是任一满足还是同时满足；
* 需要哪些数据维度；
* 维度如何覆盖目标对象；
* 哪些角色自动激活；
* 哪些角色需要申请；
* 是否存在岗位、部门或风险限制。

来源必须是：

* 权限系统；
* 功能—资源—角色配置；
* 角色 Owner；
* 业务 Owner；
* 正式制度。

## 10.4 用户实时权限状态

包括：

* 当前角色；
* 每个角色的状态；
* 生效和失效时间；
* 每个角色下的数据维度；
* 当前活动角色；
* 所属组织和 BG；
* 数据时间；
* 查询局限。

## 10.5 运行决策知识

包括：

* 工具调用条件；
* 检查顺序；
* 条件谓词；
* 分支跳转；
* UNKNOWN 处理；
* 转人工条件；
* 最大追问轮数；
* 回答所需证据门槛。

---

# 十一、核心数据结构

## 11.1 在线问题理解结果

```json
{
  "conversation_id": "CONV_001",
  "user_id": "U123",
  "topic": "权限专题",
  "l1": "7-查不到数据",
  "l2": null,
  "symptom": {
    "canonical": "用户已进入查询页面，但查不到目标公司数据",
    "original_text": "我看不到广州公司的凭证"
  },
  "user_hypotheses": [
    "可能缺少权限"
  ],
  "entities": {
    "system": "MetaERP",
    "product": null,
    "sub_product": null,
    "page_name": null,
    "url": null,
    "function_id": null,
    "business_object": "凭证",
    "target_dimensions": [
      {
        "dimension_type": "company",
        "dimension_name": "广州公司",
        "dimension_code": null
      }
    ],
    "mentioned_roles": [],
    "error_message": null
  },
  "missing_context": [
    "function_locator"
  ],
  "ambiguities": [],
  "routing_action": "COLLECT_CONTEXT"
}
```

## 11.2 对话上下文对象

```json
{
  "context_id": "CTX_001",
  "conversation_id": "CONV_001",
  "user": {
    "user_id": "U123",
    "department": null,
    "job": null,
    "bg": null
  },
  "function_locator": {
    "function_id": null,
    "resource_id": null,
    "page_name": null,
    "url": null,
    "business_object": "凭证",
    "resolution_status": "UNRESOLVED"
  },
  "target_scope": {
    "dimension_type": "company",
    "dimension_code": null,
    "dimension_name": "广州公司"
  },
  "query_context": {
    "query_conditions": {},
    "error_message": null
  },
  "collected_from": [
    {
      "field": "target_scope.dimension_name",
      "source": "USER_MESSAGE"
    }
  ]
}
```

## 11.3 问题模式对象

```json
{
  "pattern_id": "PATTERN_POC_001",
  "version": "1.0",
  "status": "APPROVED",
  "name": "指定查询功能中查不到目标公司数据",
  "scope": {
    "topic": "权限专题",
    "l1": "7-查不到数据",
    "l2": null,
    "product": "POC_SELECTED_PRODUCT",
    "sub_product": "POC_SELECTED_SUB_PRODUCT",
    "function_ids": [
      "FUNCTION_POC_001"
    ]
  },
  "symptom_matching": {
    "canonical_description": "用户已经进入指定查询页面，但查不到目标公司范围的数据。",
    "example_utterances": [],
    "negative_examples": [],
    "error_messages": []
  },
  "context_requirements": [
    {
      "requirement_id": "FUNCTION_LOCATOR",
      "any_of": [
        ["function_id"],
        ["page_name", "product"],
        ["url"],
        ["business_object", "sub_product"]
      ]
    },
    {
      "requirement_id": "TARGET_SCOPE",
      "any_of": [
        ["dimension_type", "dimension_code"],
        ["dimension_type", "dimension_name"]
      ]
    }
  ],
  "questions": [
    {
      "question_id": "ASK_FUNCTION",
      "ask_when": {
        "missing_requirement": "FUNCTION_LOCATOR"
      },
      "text": "请提供页面名称、链接，或说明您查询的是哪类业务数据。",
      "expected_fields": [
        "page_name",
        "url",
        "business_object"
      ],
      "fallback": "ESCALATE_FUNCTION_UNRESOLVED"
    },
    {
      "question_id": "ASK_TARGET_SCOPE",
      "ask_when": {
        "missing_requirement": "TARGET_SCOPE"
      },
      "text": "您需要查看的是哪个公司或组织的数据？",
      "expected_fields": [
        "dimension_name",
        "dimension_code"
      ],
      "fallback": "ESCALATE_SCOPE_UNRESOLVED"
    }
  ],
  "candidate_causes": [
    {
      "cause_code": "MISSING_ROLE",
      "priority": 10,
      "checks": [
        "CHECK_ACCESS_POLICY",
        "CHECK_USER_ROLE"
      ],
      "resolution_code": "APPLY_ROLE"
    },
    {
      "cause_code": "MISSING_DATA_SCOPE",
      "priority": 20,
      "checks": [
        "CHECK_USER_ROLE",
        "CHECK_DATA_SCOPE"
      ],
      "resolution_code": "APPLY_DATA_SCOPE"
    },
    {
      "cause_code": "EXPIRED_PERMISSION",
      "priority": 30,
      "checks": [
        "CHECK_PERMISSION_EXPIRY"
      ],
      "resolution_code": "RENEW_PERMISSION"
    },
    {
      "cause_code": "WRONG_ACTIVE_ROLE",
      "priority": 40,
      "checks": [
        "CHECK_ACTIVE_ROLE"
      ],
      "resolution_code": "SWITCH_ACTIVE_ROLE"
    },
    {
      "cause_code": "QUERY_CONDITION_ISSUE",
      "priority": 50,
      "checks": [
        "CHECK_QUERY_CONDITIONS"
      ],
      "resolution_code": "GUIDE_QUERY"
    }
  ],
  "mandatory_escalation": [
    "CONFIDENTIAL_SCOPE",
    "CROSS_BG_RESTRICTION",
    "POLICY_CONFLICT",
    "TOOL_UNAVAILABLE",
    "FUNCTION_UNRESOLVED",
    "BUSINESS_DATA_ISSUE",
    "SYSTEM_DEFECT",
    "UNKNOWN"
  ],
  "evidence": {
    "incident_ids": [],
    "reviewers": [],
    "last_verified_at": null
  }
}
```

## 11.4 可执行检查定义

问题模式中的 `checks` 不能只对应自然语言说明，必须连接到类型化检查。

```json
{
  "check_id": "CHECK_DATA_SCOPE",
  "tool": "GET_USER_ENTITLEMENTS",
  "inputs": {
    "user_id": "$context.user.user_id"
  },
  "preconditions": [
    {
      "field": "$context.target_scope.dimension_code",
      "operator": "IS_NOT_NULL"
    }
  ],
  "predicate": {
    "operator": "ROLE_DIMENSION_COVERS",
    "required_role_expression": "$access_policy.role_expression",
    "dimension_type": "$context.target_scope.dimension_type",
    "dimension_code": "$context.target_scope.dimension_code"
  },
  "on_true": {
    "result": "CAUSE_NOT_CONFIRMED"
  },
  "on_false": {
    "result": "MISSING_DATA_SCOPE"
  },
  "on_unknown": {
    "result": "ESCALATE_TOOL_OR_DATA_UNKNOWN"
  }
}
```

## 11.5 权威访问策略对象

```json
{
  "policy_id": "POLICY_POC_001",
  "version": "1.0",
  "function_id": "FUNCTION_POC_001",
  "action": "QUERY",
  "business_object": "POC_BUSINESS_OBJECT",
  "role_expression": {
    "operator": "ANY_OF",
    "roles": [
      {
        "role_id": "ROLE_POC_001",
        "role_name": "EXACT_ROLE_NAME"
      }
    ]
  },
  "dimension_requirements": [
    {
      "dimension_type": "company",
      "required": true,
      "operator": "CONTAINS_TARGET"
    }
  ],
  "eligibility": {
    "decision_mode": "AUTHORITATIVE_ONLY",
    "allowed_departments": [],
    "allowed_jobs": [],
    "requires_manual_review": false
  },
  "activation": {
    "mode": "APPLICATION",
    "application_entry": "AUTHORITATIVE_ENTRY",
    "expected_effective_time": "AUTHORITATIVE_VALUE"
  },
  "risk": {
    "level": "LOW",
    "cross_bg_allowed": false,
    "confidential_scope_allowed": false
  },
  "owner": {
    "department": "ROLE_OWNER_DEPARTMENT"
  },
  "provenance": [
    {
      "field": "role_expression",
      "source_type": "PERMISSION_SYSTEM",
      "source_id": "SOURCE_001",
      "verified_at": "YYYY-MM-DD"
    },
    {
      "field": "eligibility",
      "source_type": "ROLE_OWNER",
      "source_id": "SOURCE_002",
      "verified_at": "YYYY-MM-DD"
    }
  ]
}
```

## 11.6 用户权限状态对象

```json
{
  "user_id": "U123",
  "organization": {
    "department_id": "D001",
    "department_name": "某部门",
    "job_id": "J001",
    "job_name": "某岗位",
    "bg": "BG001"
  },
  "current_context": {
    "active_role_id": "ROLE_001",
    "active_permission_system": "PERMISSION_DOMAIN_001"
  },
  "roles": [
    {
      "role_id": "ROLE_001",
      "role_name": "EXACT_ROLE_NAME",
      "status": "ACTIVE",
      "valid_from": "YYYY-MM-DD",
      "valid_to": "YYYY-MM-DD",
      "dimensions": [
        {
          "dimension_type": "company",
          "dimension_code": "COMPANY_001",
          "dimension_name": "某公司",
          "status": "ACTIVE",
          "valid_to": "YYYY-MM-DD"
        }
      ]
    }
  ],
  "data_freshness": "YYYY-MM-DDTHH:mm:ss+08:00",
  "limitations": []
}
```

## 11.7 诊断结果对象

```json
{
  "diagnosis_id": "DIAG_001",
  "conversation_id": "CONV_001",
  "scope_status": "IN_SCOPE",
  "function_id": "FUNCTION_POC_001",
  "pattern_id": "PATTERN_POC_001",
  "result": {
    "status": "CONFIRMED",
    "cause_code": "MISSING_DATA_SCOPE",
    "confidence_mode": "DETERMINISTIC",
    "missing": [
      {
        "type": "DATA_DIMENSION",
        "role_id": "ROLE_POC_001",
        "dimension_type": "company",
        "dimension_code": "COMPANY_GZ",
        "dimension_name": "广州公司"
      }
    ]
  },
  "checks": [
    {
      "check_id": "CHECK_USER_ROLE",
      "result": "PASS",
      "evidence": "USER_PERMISSION_API"
    },
    {
      "check_id": "CHECK_DATA_SCOPE",
      "result": "FAIL",
      "evidence": "USER_PERMISSION_API"
    }
  ],
  "safety": {
    "eligibility_status": "ELIGIBLE",
    "risk_level": "LOW",
    "mandatory_escalation": false
  },
  "recommended_action": {
    "resolution_code": "APPLY_DATA_SCOPE",
    "historical_solution_class": "S2-申请数据权限（数据维度）",
    "payload": {
      "role_id": "ROLE_POC_001",
      "role_name": "EXACT_ROLE_NAME",
      "dimension_type": "company",
      "dimension_code": "COMPANY_GZ",
      "dimension_name": "广州公司",
      "application_entry": "AUTHORITATIVE_ENTRY",
      "expected_effective_time": "AUTHORITATIVE_VALUE"
    }
  }
}
```

## 11.8 决策轨迹

每次诊断必须保存：

```json
{
  "trace_id": "TRACE_001",
  "conversation_id": "CONV_001",
  "steps": [
    {
      "step": 1,
      "action": "ROUTE_TOPIC",
      "input_summary": "用户查不到目标公司数据",
      "output": "权限专题",
      "model_or_rule_version": "ROUTER_V1"
    },
    {
      "step": 2,
      "action": "RESOLVE_FUNCTION",
      "output": "FUNCTION_POC_001",
      "evidence": [
        "USER_PROVIDED_PAGE_NAME"
      ]
    },
    {
      "step": 3,
      "action": "QUERY_USER_ENTITLEMENTS",
      "tool_result_id": "TOOL_RESULT_001"
    },
    {
      "step": 4,
      "action": "CHECK_DATA_SCOPE",
      "output": "MISSING_DATA_SCOPE"
    }
  ],
  "final_result": "RECOMMEND_DATA_SCOPE_APPLICATION",
  "created_at": "YYYY-MM-DDTHH:mm:ss+08:00"
}
```

---

# 十二、权限工具和 API

## 12.1 API 一：查询用户授权

### 输入

```json
{
  "user_id": "U123",
  "permission_system": "METAERP"
}
```

### 输出要求

必须包含：

* 用户组织信息；
* 用户角色；
* 角色标准 ID 和标准名称；
* 角色状态；
* 角色有效期；
* 每个角色的数据维度；
* 维度状态和有效期；
* 当前活动角色；
* 所属 BG 或权限体系；
* 数据新鲜度；
* 查询限制。

## 12.2 API 二：定位功能

允许使用以下一种或多种输入：

```json
{
  "product": "",
  "sub_product": "",
  "page_name": "",
  "url": "",
  "button_name": "",
  "business_object": "",
  "resource_hint": ""
}
```

输出：

```json
{
  "status": "RESOLVED",
  "candidates": [
    {
      "function_id": "FUNCTION_POC_001",
      "resource_id": "RESOURCE_POC_001",
      "product": "",
      "sub_product": "",
      "page_name": "",
      "business_object": "",
      "confidence": 0.95,
      "evidence": [
        "URL_MAPPING"
      ]
    }
  ],
  "ambiguities": []
}
```

如果存在多个候选，不允许直接选择，必须追问或转人工。

## 12.3 API 三：查询访问策略

输入：

```json
{
  "function_id": "FUNCTION_POC_001",
  "action": "QUERY",
  "target_context": {
    "dimension_type": "company",
    "dimension_code": "COMPANY_GZ"
  }
}
```

输出：

* 角色逻辑表达式；
* 数据维度要求；
* 资格规则；
* 申请方式；
* 生效规则；
* 风险限制；
* 权威来源；
* 版本和核实时间。

## 12.4 API 四：访问差异诊断

长期优先建设该 API，避免由 LLM 自行比较复杂权限对象。

输入：

```json
{
  "user_id": "U123",
  "function_id": "FUNCTION_POC_001",
  "action": "QUERY",
  "target_context": {
    "company": "COMPANY_GZ"
  }
}
```

输出：

```json
{
  "decision": "DENY",
  "reason_codes": [
    "MISSING_DATA_SCOPE"
  ],
  "missing": [
    {
      "type": "DATA_DIMENSION",
      "role_id": "ROLE_POC_001",
      "dimension_type": "company",
      "dimension_code": "COMPANY_GZ"
    }
  ],
  "satisfied_conditions": [
    "ROLE_PRESENT",
    "ROLE_ACTIVE"
  ],
  "unknown_conditions": [],
  "policy_id": "POLICY_POC_001",
  "policy_version": "1.0",
  "data_freshness": "YYYY-MM-DDTHH:mm:ss+08:00"
}
```

`decision` 必须支持：

```text
ALLOW
DENY
UNKNOWN
NOT_APPLICABLE
```

不得只返回 `true` 或 `false`。

## 12.5 工具异常处理

出现以下任一情况，Agent 不得输出确定性权限建议：

* API 超时；
* 用户权限数据过旧；
* 访问策略不存在；
* 功能存在多个候选；
* 角色名称无法唯一匹配；
* 数据维度代码无法解析；
* 权限 API 和角色目录冲突；
* 返回 `UNKNOWN`；
* 权威来源已经过期。

---

# 十三、知识生产流程

## 13.1 工单的用途

历史工单用于提供：

* 用户真实表达；
* 常见页面和功能描述；
* 角色和维度名称线索；
* 候选问题模式；
* 客服追问经验；
* 已发生的解决动作；
* 转人工原因；
* 回放和评测样本。

工单不能单独提供：

* 完整功能—角色映射；
* 正式资格规则；
* 当前用户权限；
* 完整岗位—角色关系；
* 角色风险等级；
* 当前有效申请入口；
* 未出现过的合法角色；
* 权威生效时间。

## 13.2 工单抽取原则

LLM 从工单抽取知识时必须遵守：

### 禁止抽象化

禁止生成：

```text
申请相应权限
补充对应维度
联系相关部门
```

必须保留原始角色、维度、页面、报错和动作名称。

### 禁止无来源

所有抽取字段必须能够追溯到工单 ID 或其他来源。

### 禁止推断

工单没有明确说明的事实填 `null` 或标记为候选，不能由模型补全。

### 禁止把高频当权威

工单出现次数只说明常见，不代表权限规则正确。

## 13.3 生产流程

```text
历史工单筛选
      ↓
逐条结构化抽取
      ↓
字段级来源绑定
      ↓
按子产品、功能、L1 和解决结果聚合
      ↓
形成候选问题模式
      ↓
人工审核候选原因和追问
      ↓
连接权限系统和 Owner 权威事实
      ↓
形成可执行检查
      ↓
回放验证
      ↓
批准发布
```

## 13.4 知识状态

每个知识对象必须有以下状态之一：

```text
CANDIDATE
UNDER_REVIEW
APPROVED
CONFLICT
DEPRECATED
EXPIRED
```

只有 `APPROVED` 状态可以驱动确定性建议。

## 13.5 来源可信等级

```text
SYSTEM_VERIFIED
OWNER_VERIFIED
POLICY_VERIFIED
HUMAN_REVIEWED
MULTI_CASE_SUPPORTED
SINGLE_CASE_HINT
UNVERIFIED
CONFLICT
DEPRECATED
```

以下字段必须至少达到 `SYSTEM_VERIFIED`、`OWNER_VERIFIED` 或 `POLICY_VERIFIED`：

* 功能—角色映射；
* 角色申请资格；
* 跨 BG 规则；
* 涉密规则；
* 申请入口；
* 角色是否自动激活；
* 角色风险等级。

---

# 十四、数据集与金标准

## 14.1 数据筛选

从历史工单中筛选：

```text
专题 = 权限专题
+
目标子产品
+
目标功能或业务对象
+
L1 = 7-查不到数据
```

同时加入以下对照案例：

* 相似表象但属于业务数据异常；
* 相似表象但属于查询条件问题；
* 相似表象但属于后台配置；
* 相似表象但属于缺陷；
* 涉密和跨 BG；
* 功能无法定位；
* 用户权限状态和历史解决方案冲突；
* 非权限专题案例。

## 14.2 数据集拆分

建议建立四组数据。

### 设计集

约 30 条。

用于：

* 手工理解问题；
* 设计首个问题模式；
* 识别所需字段；
* 设计工具契约。

### 开发集

约 60 条。

用于：

* 调试提示词；
* 调整追问；
* 调整规则顺序；
* 补充术语和别名；
* 修复工具映射。

### 盲测集

不少于 80 条。

在开发完成前冻结，开发人员不可根据个案修改规则。

用于正式验收。

### 安全与反例集

不少于 30 条。

必须包含：

* 涉密；
* 跨 BG；
* 高风险角色；
* 权威规则缺失；
* 工具失败；
* 多功能歧义；
* 用户误判权限；
* 实际业务数据异常；
* 疑似缺陷；
* 对抗性或信息不足表达。

## 14.3 历史回放等级

由于历史工单未必保存当时权限快照，案例分为：

| 等级 | 条件             | 用途           |
| -- | -------------- | ------------ |
| A  | 有当时权限快照或完整授权日志 | 端到端诊断评测      |
| B  | 工单明确记录角色、维度和状态 | 端到端诊断评测      |
| C  | 可由人工结合多项证据重建   | 经审核后用于评测     |
| D  | 无法还原当时权限状态     | 只用于问题理解和路由评测 |

不得使用用户当前权限状态直接替代历史权限状态。

## 14.4 金标准字段

每条案例至少标注：

```text
事件单 ID
用户原始问题
正确专题
正确 L1/L2
目标产品和功能
目标业务对象
目标数据维度
最低必要上下文
应该提出的追问
当时用户角色状态
当时数据维度状态
真实原因
应排除原因
正确解决动作
精确角色
精确维度
是否具备申请资格
是否必须转人工
不允许采取的动作
最终是否解决
证据来源
标注人
复核人
```

## 14.5 数据泄漏控制

盲测时不得提供：

* 历史解决方案字段；
* 最终 S 分类；
* 人工处理结论；
* 与答案直接相关的标签；
* 同一事件的关联解决工单；
* 从解决方案中提取的角色或维度。

问题模式和术语库中也不得直接收录盲测案例的完整解决文本。

---

# 十五、回答规范

## 15.1 确定性建议

只有同时满足以下条件时，Agent 才能给出确定性建议：

1. 功能已经唯一定位；
2. 目标数据范围已经明确；
3. 用户权限状态查询成功；
4. 访问策略为批准状态；
5. 确定性检查返回明确原因；
6. 资格和安全校验通过；
7. 申请入口和动作信息已核实。

建议输出结构：

```text
当前判断
判断依据
用户现有权限状态
缺失或错误的具体对象
建议采取的动作
申请或操作入口
预计生效时间
生效后的验证方法
```

## 15.2 候选性建议

如果只有历史模式支持，但没有实时权限或权威映射，必须使用候选表达：

> 根据该问题的常见处理方式，可能与目标公司的数据维度有关。目前无法确认您是否已经具备该范围，需要先查询权限状态或由客服核实。

不能说：

> 您缺少广州公司的数据权限。

## 15.3 转人工回答

转人工时必须说明：

* 为什么不能继续判断；
* 已经核实了什么；
* 还缺少什么；
* 将转给哪个团队；
* 用户不需要重复提供哪些信息。

## 15.4 禁止回答

禁止：

* 推荐模糊的“相应角色”；
* 修改精确角色名称；
* 把角色和数据维度混为一谈；
* 根据岗位猜测用户一定有资格；
* 根据一条工单推导正式规则；
* 工具失败后继续输出确定结论；
* 将用户猜测当作事实；
* 对涉密、高风险和跨 BG 权限提供确定申请建议。

---

# 十六、评测指标

## 16.1 问题理解指标

* 专题准确率；
* POC 范围识别准确率；
* L1/L2 准确率；
* 功能定位准确率；
* 业务对象识别准确率；
* 目标维度识别准确率；
* 用户猜测与客观表象分离准确率。

## 16.2 追问指标

* 必要追问召回率；
* 不必要追问率；
* 首个追问有效率；
* 平均追问轮数；
* 追问后上下文满足率；
* 超过最大追问轮数比例。

## 16.3 诊断指标

* 原因代码准确率；
* 缺角色识别准确率；
* 缺维度识别准确率；
* 权限过期识别准确率；
* 当前角色错误识别准确率；
* 查询条件问题识别准确率；
* 多原因混淆率；
* `UNKNOWN` 使用准确率。

## 16.4 动作指标

* 精确角色推荐准确率；
* 精确维度推荐准确率；
* 申请入口准确率；
* 续期动作准确率；
* 切换角色指引准确率；
* 验证步骤完整率；
* 不必要申请率；
* 重复申请率。

## 16.5 安全指标

* 越权建议率；
* 不合资格推荐率；
* 涉密权限误推荐率；
* 跨 BG 权限误推荐率；
* 高风险权限误推荐率；
* 无权威来源确定回答率；
* 工具失败后错误回答率；
* 应转人工但未转人工率。

## 16.6 转人工指标

* 必须转人工召回率；
* 不必要转人工率；
* 转人工原因准确率；
* 转人工摘要完整率；
* 人工接手后重复追问率；
* 人工处理时长变化。

## 16.7 业务指标

* Agent 用户确认解决率；
* 自动闭环率；
* 权限人工介入率；
* 平均解决时长；
* 同一用户同一问题短期重复开单率；
* 用户满意度；
* 单次成功解决成本；
* 人工工单标准化下降比例。

---

# 十七、POC 验收门槛

以下为建议的 Go/No-Go 门槛。

## 17.1 安全门槛

盲测集必须满足：

```text
越权建议率 = 0
涉密或高风险权限误推荐率 = 0
工具失败后仍输出确定性建议 = 0
无权威来源的确定性角色推荐 = 0
```

任一安全指标不满足，不得进入用户灰度。

## 17.2 诊断门槛

在“系统允许自动解决”的案例中：

```text
原因诊断准确率 ≥ 90%
精确角色或维度动作准确率 ≥ 95%
功能唯一定位准确率 ≥ 95%
必要上下文满足率 ≥ 90%
```

角色或维度推荐必须按照标准 ID 和标准名称精确匹配，不能只做语义相似匹配。

## 17.3 转人工门槛

```text
必须转人工召回率 ≥ 95%
安全类必须转人工召回率 = 100%
转人工摘要完整率 ≥ 90%
```

## 17.4 线上业务门槛

灰度阶段建议满足：

```text
确定性建议后的用户确认解决率 ≥ 70%
同类重复开单率不高于人工基线
用户投诉或安全事件 = 0
人工接手后的重复追问明显下降
```

自动闭环率不是安全门槛。Agent 可以保守转人工，但不能为了提高闭环率输出不可靠建议。

---

# 十八、实施阶段

## 阶段 0：范围冻结与可行性确认

### 目标

确定唯一 POC 切片，并确认其权威数据可获得。

### 工作内容

1. 统计权限专题下子产品、功能、L1 和 S 分类分布；
2. 筛选候选高频查询功能；
3. 确认功能—角色—维度关系是否可获取；
4. 确认用户权限状态是否可查询；
5. 确认角色 Owner 和业务 Owner；
6. 排除涉密、高风险和跨 BG 场景；
7. 冻结 POC-QueryScope；
8. 明确用户入口和转人工团队。

### 退出条件

只有满足以下条件才进入下一阶段：

* 唯一功能已冻结；
* 目标维度已冻结；
* 至少一个权威访问策略来源已确认；
* 用户权限数据能够获得；
* 至少存在一批可回放案例；
* 业务和角色 Owner 已确认参与。

### 交付物

* POC 范围说明；
* 候选功能对比；
* 最终功能和维度定义；
* 权威数据来源清单；
* 参与团队清单。

---

## 阶段 1：金标准与人工决策原型

### 目标

不开发完整系统，先验证问题模式和决策链是否成立。

### 工作内容

1. 建设设计集和开发集；
2. 人工还原每条案例的真实原因；
3. 标注必要追问和不允许动作；
4. 手工编写一个完整问题模式；
5. 手工建立目标功能访问策略；
6. 使用人工准备的用户权限 Fixture；
7. 模拟 Agent 按状态机完成诊断；
8. 记录无法判断的原因。

### 退出条件

* 一个问题模式可以覆盖主要已知分支；
* 所有检查都有明确输入和结果；
* 自动解决与转人工边界清晰；
* 不依赖历史解决方案泄漏；
* 所需权限字段已经完整列出。

### 交付物

* 问题模式 V0.1；
* 访问策略 V0.1；
* 金标准数据 Schema；
* 人工 Fixture；
* 失败原因清单；
* API 最小字段清单。

---

## 阶段 2：离线系统原型

### 目标

完成端到端离线 Agent。

### 工作内容

1. 开发在线专题路由；
2. 开发问题理解和实体抽取；
3. 开发上下文管理和追问；
4. 开发功能定位；
5. 建设问题模式服务；
6. 接入或模拟用户权限查询；
7. 实现确定性诊断规则；
8. 实现安全与转人工规则；
9. 实现回答生成；
10. 保存完整决策轨迹。

### 交付物

* 离线 Agent；
* 工具契约；
* 决策规则；
* 审计日志；
* 方案 A/B/C 对比环境；
* 开发集评测报告。

---

## 阶段 3：盲测验收

### 目标

在冻结数据上评估真实效果。

### 工作内容

1. 冻结模型、规则和知识版本；
2. 在盲测集和安全集上运行；
3. 对比相似工单基线、问题模式方案和目标方案；
4. 统计分支级指标；
5. 人工复核所有错误和高风险输出；
6. 将失败原因分类为：

   * 问题理解；
   * 功能定位；
   * 上下文不足；
   * 知识缺失；
   * 权威数据缺失；
   * API 问题；
   * 规则错误；
   * LLM 推理错误；
   * 回放数据不足；
   * 应转人工范围。

### 退出条件

只有同时满足安全、诊断和转人工门槛，才能进入线上灰度。

### 交付物

* 盲测报告；
* 安全评测报告；
* 基线对比报告；
* Go/No-Go 结论；
* 未解决问题清单。

---

## 阶段 4：受控线上灰度

### 目标

验证真实用户环境中的解决效果。

### 灰度限制

* 只开放已冻结的单一功能；
* 只覆盖低风险角色和维度；
* 只提供建议，不自动申请；
* 工具失败立即转人工；
* 所有回答保存决策轨迹；
* 支持人工一键接管；
* 支持用户反馈“已解决 / 未解决”；
* 首期对用户数量和组织范围进行限制。

### 观察指标

* 用户确认解决率；
* 转人工率；
* 重复开单率；
* 人工接手效率；
* 错误建议；
* 用户投诉；
* 安全事件；
* 工具稳定性；
* 知识缺失类型。

### 退出条件

* 安全事件为零；
* 用户确认解决达到门槛；
* 重复开单未恶化；
* 人工接手信息完整；
* 权限系统和知识更新流程能够持续运行。

---

# 十九、交付物清单

POC 必须交付：

1. POC 范围定义；
2. 在线专题路由规范；
3. 在线问题理解 Schema；
4. 对话上下文 Schema；
5. 问题模式 Schema；
6. 首个批准的问题模式；
7. 术语和别名表；
8. 目标功能权威访问策略；
9. 用户权限查询 API 契约；
10. 功能定位 API 契约；
11. 权限差异检查规则；
12. 安全与转人工规则；
13. 金标准数据集；
14. 盲测集和安全集；
15. Agent 离线原型；
16. 决策轨迹和审计日志；
17. 基线对比报告；
18. 盲测验收报告；
19. 灰度方案；
20. 知识审核和更新机制；
21. Go/No-Go 决策报告；
22. 下一阶段能力缺口清单。

---

# 二十、团队职责

| 角色       | 主要职责                   |
| -------- | ---------------------- |
| 产品经理     | 冻结 POC 范围、用户流程、业务指标和交互 |
| 客服专家     | 还原真实处理过程、设计追问、审核问题模式   |
| 权限平台     | 提供用户授权数据、角色配置和访问策略     |
| 角色 Owner | 确认角色功能、申请资格、风险和入口      |
| 业务 Owner | 确认查询规则、业务对象和非权限原因      |
| 数据团队     | 工单筛选、数据脱敏、金标准建设        |
| Agent 研发 | 在线理解、对话编排、工具调用和回答生成    |
| 后端研发     | API、确定性检查、审计日志和系统集成    |
| 测试团队     | 盲测、安全测试、回归测试和工具故障测试    |
| 安全团队     | 审核高风险边界、越权风险和日志合规      |
| 项目负责人    | 里程碑、依赖协调和 Go/No-Go 决策  |

---

# 二十一、知识治理

## 21.1 知识 Owner

每类知识必须明确 Owner：

| 知识类型    | Owner         |
| ------- | ------------- |
| 用户表达与术语 | 客服与产品         |
| 问题模式    | 客服专家与产品       |
| 功能—角色映射 | 权限平台或角色 Owner |
| 资格规则    | 角色 Owner      |
| 查询操作规则  | 业务 Owner      |
| 用户实时权限  | 权限系统          |
| 安全限制    | 安全和权限平台       |
| 转人工路由   | 客服运营          |

## 21.2 更新机制

知识对象必须包含：

* 版本；
* 状态；
* Owner；
* 权威来源；
* 生效日期；
* 最后核实日期；
* 下次复核日期；
* 修改记录；
* 受影响评测案例。

角色、入口或规则发生变化后，必须触发：

1. 知识更新；
2. 相关问题模式重新审核；
3. 盲测集回归；
4. 已发布版本替换；
5. 旧版本标记失效。

## 21.3 冲突处理

发生以下冲突时不得由模型自行裁决：

* 工单与系统配置冲突；
* 系统配置与 Owner 说法冲突；
* 两个 Owner 说法冲突；
* 不同版本规则冲突；
* 同名角色存在不同定义。

处理顺序：

```text
暂停确定性建议
→ 标记 CONFLICT
→ 路由至明确 Owner
→ 人工裁决
→ 更新权威来源
→ 回归测试
→ 重新发布
```

---

# 二十二、主要风险与控制措施

## 风险一：POC 范围再次扩大

### 表现

逐步加入多个子产品、多个页面、多个维度和多类权限问题。

### 控制

* POC 范围必须由唯一 Scope ID 管理；
* 新功能只能进入后续 Backlog；
* 盲测前不得扩范围；
* 线上路由严格校验是否属于 POC。

## 风险二：用工单代替权威权限事实

### 表现

根据历史工单直接认定功能需要某角色，或用户具备某资格。

### 控制

* 权限关键字段必须有权威来源；
* 工单只能作为候选线索；
* 未核实映射不得用于确定性建议。

## 风险三：LLM 猜测权限差异

### 表现

模型自行比较复杂角色和维度对象，产生漏判或误判。

### 控制

* 使用类型化谓词和确定性检查；
* 长期提供访问差异诊断 API；
* `UNKNOWN` 必须安全降级。

## 风险四：历史回放失真

### 表现

使用当前权限状态替代历史权限状态。

### 控制

* 建立 A/B/C/D 回放等级；
* 只有可重建权限状态的案例用于端到端评测；
* 其余只评问题理解。

## 风险五：追问过多

### 表现

Agent 像表单一样连续询问用户。

### 控制

* 优先使用系统上下文；
* 每次询问最高信息增益字段；
* 设置三轮上限；
* 支持 URL、页面名、业务对象等替代定位。

## 风险六：为了自动闭环率降低转人工

### 表现

Agent 在证据不足时仍然给出建议。

### 控制

* 安全指标优先于闭环率；
* 必须转人工召回率作为验收门槛；
* 无证据确定回答计为严重错误。

## 风险七：用户执行建议后仍未解决

### 表现

权限已经补充，但数据仍不可见。

### 控制

* 每个动作包含验证步骤；
* 用户可以反馈未解决；
* 再次诊断时排除已完成动作；
* 第二次仍无法判断时转人工；
* 保存前后权限状态。

## 风险八：知识更新后行为漂移

### 控制

* 版本化知识和策略；
* 保存运行版本；
* 更新触发回归；
* 线上灰度发布；
* 支持快速回退。

---

# 二十三、Go/No-Go 决策

## Go

满足以下条件，可以进入小范围线上灰度：

* POC 范围完全冻结；
* 权威访问策略已确认；
* 用户权限查询稳定；
* 安全门槛全部通过；
* 自动解决案例达到诊断门槛；
* 必须转人工召回达到门槛；
* 所有回答可追溯；
* 人工接管链路可用；
* 业务和权限 Owner 同意上线。

## Conditional Go

出现以下情况，可以继续离线迭代，但不能直接上线：

* 诊断准确率接近门槛；
* 权限 API 偶发不稳定；
* 功能定位仍存在少量歧义；
* 转人工摘要尚需优化；
* 部分权威字段需要人工 Fixture。

## No-Go

出现以下任一情况，应暂停上线：

* 发生越权建议；
* 涉密或高风险误推荐；
* 无权威来源仍输出确定角色；
* 工具失败后继续给出确定建议；
* 无法获得用户实时权限；
* 功能—角色关系无法核实；
* 历史案例无法建立可靠金标准；
* 转人工链路不可用；
* 盲测集被开发数据污染。

---

# 二十四、POC 后的扩展顺序

POC 通过后，建议按以下顺序扩展：

1. 同一功能下的其他高频问题模式；
2. 同一子产品下的其他查询功能；
3. L1=2b 不知道申请什么权限；
4. L1=3a 提示无权限；
5. L1=3b 按钮或入口不可用；
6. L1=8 权限规则咨询；
7. 更多维度类型；
8. 更多角色组合；
9. 跨 BG 场景；
10. 涉密和高风险权限；
11. 权限申请自动发起；
12. 与业务数据和缺陷诊断工具集成。

扩展依据必须来自：

* 工单量；
* 当前转人工原因；
* 权威数据完整度；
* 安全风险；
* 自动化价值；
* 用户反馈。

---

# 二十五、最终项目结论

本 POC 建设的不是一份供 Agent 检索的权限 FAQ，也不是一次完整权限知识图谱建设。

POC 的正式定义是：

> 选择一个高频、低风险、权威信息可获得的权限查询场景，建立“用户表象—必要上下文—功能定位—候选原因—确定性检查—安全校验—解决动作—验证方法—转人工条件”的端到端诊断能力，并通过历史盲测和受控线上灰度，验证 Agent 是否能够安全降低人工客服介入。

POC 成功不意味着 Agent 能处理全部权限问题。

POC 成功意味着已经证明：

1. 一个权限问题可以被建模为可执行的诊断过程；
2. 工单经验、权威规则和实时状态可以被正确分工；
3. LLM 可以承担理解、追问、编排和解释；
4. 确定性系统可以承担权限计算和安全判断；
5. Agent 能在可解决时给出精确动作；
6. Agent 能在不可解决时停止猜测并完成高质量转人工；
7. 该模式具备向更多功能和问题类型复制的基础。
