先说一个可能改变方案形态的判断：**你现在描述的不是"问答系统"，而是"小标签集上的路由 + 澄清"问题。** 这个 reframe 会让评测和工程量都缩小一个量级。

## 1. 真正的答案空间是 SOP 链接集合，不是自由文本

200 多条数据里的链接去重后，大概率只有 20–40 个 SOP。那就是你的 label space。任务变成：

> 用户问题 → { 选中某个 SOP | 信息不足，需要问 X | 不在覆盖范围，转人工 }

这意味着你不需要评"生成质量"，只需要评三分类的正确率。第一天就能跑出 baseline。

## 2. 数据的性质：标签是噪声的，缺的是"澄清"不是"答案"

电话没留痕这件事的后果不是"答案缺失"，而是**输入被截断了**——专员在电话里做的意图消歧没有被记录，只留下了消歧之后的结果。所以 (标题 → 链接) 这个 pair 里隐含了一段你看不见的对话。

直接拿它当训练/评测标签，会系统性高估模型能力（因为链接是"知道答案后"给的），同时低估澄清的必要性。

**必须先做的分层标注**（200 条，人工或 LLM 预标+复核，半天量级）：

- **A 类**：只看标题就能唯一确定 SOP
- **B 类**：标题不足，但"问题描述/问题原因"补上就能确定 → 这批是金矿，**缺的那部分信息就是该问用户的澄清问题**，可以直接反推出澄清话术
- **C 类**：全字段也定不了，或处理结果里根本没链接

A:B:C 的比例决定项目形态。这个比例是你要测的**第一个数字，而不是模型能力**。

## 3. 三个被低估的字段

**"解决办法（功能/数据）"这个二分基本等于"可自动化性"标记。** 功能类 = 用户不会操作，SOP 能解决；数据类 = 要后台改配置/补数据，SOP 给了也没用，必须转人工。MVP 应该只覆盖功能类，其余直接路由到人工——这一刀切下去，问题规模会明显变小。

**"问题表象"可以直接当意图 taxonomy 的候选**，但你需要先建一张 `表象 × SOP` 的映射矩阵。如果某个表象对应多个 SOP，那就是天然的歧义点，而**区分这几个 SOP 所需的判别信息，就是澄清问题的内容**。澄清逻辑可以从矩阵里半自动生成，不用凭空设计。

**多租户 + RBAC 是隐藏的歧义源，但也是免费的消歧信号。** 同一句"申请 XX 权限"，对租户管理员的正确答案是"去授权"，对普通用户是"去发起申请"。这个信息不在文本里，在登录态里。租户已开通的子产品/模块同理。**很多"信息不完整"其实不需要问用户，从当前会话上下文就能补全**——这也正好和你已有的页面级帮助打通：页面上下文本身就是 disambiguation signal，两套东西不该重复建设。

## 4. LLM Wiki 的同步问题：做成 pointer-only

你担心的"原内容更新 wiki 也要更新"，解法是**不要把 SOP 正文沉淀进 wiki**。Wiki 层只存路由元数据：

```
intent_id / 问题表述变体 / 适用条件(角色·模块·租户类型)
/ 判别所需信息 / → link
```

正文永远以链接为 source of truth（要么实时抓，要么 RAG 建索引但不作为答案来源）。这样 SOP 改步骤时你什么都不用动，只有它的**适用范围或标题变了**才需要维护路由表。如果你把摘要写进 wiki，就引入了双写一致性问题，长期一定会腐烂。

配套需要的只有两个机制：死链/更新时间监测，以及**覆盖缺口报表**（高频问题没有对应 SOP → 反向驱动内容生产）。后者本身就是可以单独交付的价值。

## 5. 第一周的动作（全是数据侧，不碰模型）

1. SOP 链接去重，建 inventory；统计 200 条里"处理结果含链接"的比例
2. 标 A/B/C 分层 + 过滤掉"数据类"
3. Baseline 实验：只给标题 + SOP 列表（标题+摘要），让模型输出 top-1 或"信息不足+要问什么"
4. 对照实验：给全字段。**A/B 两组的 gap = 澄清机制能带来的收益上限**

注意评测集的 ground truth 要人工重标，至少抽样验证——专员给的链接不一定唯一或最优，直接当 label 会污染结论。

另外，**只取一个月是采样不足**。3000/月里筛权限，多拉几个月能上千条，长尾覆盖和意图分布估计才站得住。

## 6. 明确的停止条件

先把这几条写下来，免得后面沉没成本上来了不好退：

- 去重后 SOP < 15 个且长尾极短 → 做个静态 FAQ 或页面级帮助入口就够了，不需要 LLM
- C 类 > 40% → 瓶颈在工单记录流程（电话不留痕），应该先改流程让专员回填，建模是治标
- A 类 baseline 已经 >90% → 意图识别不是难点，难点在 SOP 覆盖度，重心应转向内容生产

最后一个提醒：权限场景下**答错的代价远大于不答**。评测不要只看 accuracy@1，要拆成"正确答 / 正确澄清 / 错答"三档，并且给错答单独设阈值。信息不足时的正确行为是问，不是猜。

想先从哪块深入？我觉得 A/B/C 分层的标注口径和 `表象 × SOP` 矩阵这两件事最值得先把定义抠清楚。