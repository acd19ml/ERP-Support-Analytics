先把你这个需求拆干净——你现在其实混在一起讲了三件事,分开之后每件事的动作和验收标准都不一样:

**一、这是三个子问题,不是一个**

1. **知识资产梳理**:把 200+ 条工单 + 人工维护的 SOP blog 沉淀成 LLM 可用的知识层(你说的 llm wiki),并解决与源内容的同步问题。
2. **Baseline 能力验证**:当前模型(裸模型 or 模型+wiki 检索)对这批问题能否给出正确的"文本+链接"回复。这是个评测工程问题,前提是先有 ground truth。
3. **不完整问题的处理策略**:多轮电话沟通没留痕导致的语义残缺。这个不是数据清洗问题,是产品策略问题——线上真实用户提问同样模糊,所以"追问补全"本身应该是系统能力的一部分,而不是把模糊数据洗掉。

顺序上必须 1 → 2,而 3 决定了 2 的评测怎么设计。

**二、先做数据画像(第一个具体动作)**

在写任何 prompt 之前,先对 200 条做统计,这一步一天内能出结果:

- 解决方案中**含链接的比例**;链接去重后有多少个**独立 SOP 文档**(我猜远小于 200,可能就 20–40 个高频流程,这直接决定 llm wiki 的规模和构建成本)
- 问题描述(专员注释)的缺失率——这是判断"哪些条目语义可恢复"的关键字段
- 按"问题表象"多分类 + "功能/数据"二分做交叉分布。**功能/数据这个二分可能是你最值钱的路由信号**:功能类(不会操作→给 SOP 链接)适合 llm wiki 方案;数据类(权限配置本身有问题→需要人工/工单)天然不该由 wiki 回答,应该走转人工。如果这个假设成立,方案边界就清晰了。

**三、用 RBAC 结构定义"问题完整度"**

RBAC 给了你一个天然的 slot schema:**主体(用户/角色)× 客体(子产品/模块/应用/资源)× 诉求类型(申请/报错无权限/审批卡住/权限异常)**。一条问题是否"可直接映射到 SOP",就看这几个槽位填了几个。这样你可以把 200 条客观地分成三层:

- **A 层**:槽位齐全 + 解决方案含链接 → 直接构成 (问题, SOP) 标注对,是评测集主体
- **B 层**:槽位缺失但解决方案明确 → ground truth 是"应追问哪个槽位",测的是澄清能力而非回答能力
- **C 层**:解决方案本身没沉淀(纯电话解决无记录)→ 承认不可用,不要硬标

这个分层比"模糊/清晰"的主观判断可复现,而且 A 层占比直接就是你向上汇报时"该方案可覆盖率"的初始估计。

**四、llm wiki 的最小结构**

每个 wiki entry 以**去重后的 SOP 链接**为单位(不是以工单为单位),包含:意图描述(用真实工单标题做同义改写来源)、适用条件(哪些子产品/模块,多租户下是否通用)、操作步骤摘要、原链接、源文档更新时间。真实工单标题很重要——它们是用户真实语言分布,比 PM 写的规范描述更适合做检索召回的锚点。

同步问题现在就要问清楚,不要留到后面:**blog 平台有没有更新时间元数据或变更通知能力?** 如果有,定期 diff + 触发重摘要就行;如果没有,llm wiki 会静默腐烂,这是方案的长期运维死穴,提前写进风险。

**五、预注册的失败模式(建议 baseline 评测时专门构造)**

1. **同名权限跨子产品歧义**:用户说"申请 XX 权限"但没说哪个应用,存在多个同名 SOP → 模型是否会自信地给错链接而不是追问。这是文本+链接方案最危险的失败,因为回复看起来完全合理。
2. **功能/数据误路由**:数据类问题(权限配的确实不对)被当成功能类回了 SOP,用户照着操作也解决不了,产生二次工单。
3. **多租户差异**:同一流程不同租户审批链不同,SOP 是否 per-tenant?如果是,检索层需要租户维度过滤。

**六、你需要向业务方确认的问题**

评测成功标准是什么(链接命中率?还是二次进线率下降?);SOP blog 的维护 owner 和更新频率;模糊工单能否找专员做小规模回访补标(哪怕 30 条,B 层的 ground truth 就有了);多租户下权限流程的差异度。

如果这个梳理方向没问题,下一步我可以帮你把 slot schema 和分层标注的具体规则写出来,或者设计 A 层的 baseline 评测方案(指标定义 + 对照组设置)。你手上如果能脱敏导出几条典型数据,拿真实样例过一遍会比抽象讨论准得多。


----------------------------------------

按你要的分法整理。先说一个总原则,能解开你一半的困惑:**抽取阶段不要求你懂映射关系,只要求你忠实记录专员说了什么**。"专员面对现象 X 给出了答案 Y"本身就是知识,Y 到底是角色还是数据维度、为什么是 Y 不是 Z,可以后补。所以抽取脚本现在就能写,不被这些疑问阻塞。

---

**一、我可以帮你解答的**

**1. 数据维度权限要不要抽——要,而且不需要你先分清楚**

抽取 schema 不要设计成"只抽三元组",设计成:

```
现象/诉求(用户想干什么) | 专员给出的权限项名称(原文照抄,可多个) | 
权限类型(角色/数据维度/不确定) | 页面标识(URL或app标识,可空) | 
动作类型(申请权限/切角色/其他) | SOP链接(可空) | case_id
```

关键是**允许"不确定"**。"WOM 变更任务令_计划员"你拿不准是"数据维度+角色"还是一个完整角色名——就整串照抄,类型标"不确定"。抽完之后把所有权限项名称去重排序,你会得到一张几十个名字的清单,拿这张清单去问 PM/专员逐个定性,比在抽取时逐条纠结高效得多。顺便说我的判断供你参考:**"WOM 变更任务令_计划员"更可能是一个完整的细粒度角色名**(下划线命名是权限系统里角色/权限项的典型风格,"业务对象_岗位"是常见的角色命名模式),而"CGB 订单信息查询"这种更像数据维度权限项——但这正是要拿清单去确认的,不要信我,信权限系统的实际定义。

**2. 没有 URL 的记录不是过滤掉,是降级使用**

"用户说查不到销售订单、专员没记 URL 就给了方案"——这条对**映射表**贡献不完整(缺页面键),但它仍然贡献两样东西:一是 (现象描述 → 权限项) 的弱映射,"查询不到销售订单"这个表述本身可以作为检索锚文本挂在对应权限项上;二是它属于评测集 B 层——线上遇到同样模糊的问题,正确行为就是追问"请提供页面链接"。所以处理方式是:抽取时页面标识留空、打个 `页面缺失` 标记,不删。真正要过滤的只有 C 层(下面第 4 点)。

**3. 切角色 vs 申请角色的判定逻辑,可以先想清楚再去问**

这两者的区别在事实层面很清晰:**用户已有该角色但当前会话没启用(或以访客身份登录)→ 切角色;用户没有该角色 → 申请**。所以判定需要的上下文是"该用户当前持有哪些角色"——这是运行时数据,不在工单文本里,也永远不可能靠语义识别出来。由此推出系统设计上只有三种可能:(a) 系统能查用户当前角色(有 API),那这是个查询后分支;(b) 查不了,那回复策略只能是二段式话术:"如果你已有 XX 角色请切换,没有则去申请";(c) 把"是否访客登录/是否已有角色"作为追问项。**你去开会不是问"我怎么知道该回哪个",而是问"有没有接口能查用户角色"——问题就从开放讨论变成了一个是非题**。另外注意:专员记录里"访客登录"出现,说明"检查登录身份"可能应该是所有权限类回复的前置提示,这是个很便宜的产品改进,可以顺手提。

**4. "帮忙看下"类:是 C 层,但别白扔**

标题无信息、无截图、Cause 里直接出结论("这是工单主数据的权限")——说明信息走了电话/屏幕共享等未记录通道。这类数据对训练和评测都不可用,归 C 层排除。但要做一件事:**统计 C 层占比**。这个数字开会要讲,因为它是"任何自动化方案的天花板损耗",也是对"客服记录规范"提改进要求的依据(比如要求专员必须回填页面链接)。C 层比例如果有 15–20%,这本身就是汇报里的重要一页。

**5. "好几个角色都有这个权限,推荐哪个"——你可以先建立预期答案**

这个要确认(见下面),但成熟权限体系通常有**最小权限原则**:推荐能满足诉求的权限范围最小的那个角色,或者直接申请数据维度权限而不是大角色。也可能存在组织规则(某些角色限定岗位/部门才能申请)。你带着"是否按最小权限推荐、有没有岗位限制"这个具体问法去,对方好回答。还有一种现实可能:**专员自己也没有标准,是凭经验随手推荐的**——如果是这样,你的映射表反而会成为第一份标准,这对你的项目是加分项而不是阻塞。

---

**二、需要开会和相关人员确认的(建议按这个顺序问)**

给权限系统 owner / 专员团队的:

1. **权限项命名规则**:拿着你去重后的权限项清单问——"WOM 变更任务令_计划员"是一个独立角色,还是"角色+数据范围"的组合?角色和数据维度权限在 iAuth 里是两类不同的申请对象吗?申请入口一样吗?(这决定你映射表里"权限类型"字段的取值和答案话术)
2. **一权多角色时的推荐规则**:同一操作权限包含在多个角色里时,专员按什么规则推荐?有最小权限原则吗?有岗位/部门申请限制吗?还是纯凭经验?
3. **能否查询用户当前角色**:有没有 API 或页面能查"某用户当前持有/启用的角色"?智能客服/帮助系统有没有权限调用?(决定切角色 vs 申请的分支怎么实现)
4. **角色名的租户/组织差异**:同一个页面,不同租户或组织要申请的角色名一样吗?(决定映射表要不要加租户维度)

给 PM / 客服管理方的:

5. **记录规范可否改进**:能否要求专员今后必须回填用户的页面链接/截图?(降低未来 C 层比例,也是给你的数据管道续命)
6. **SOP 文档(KT 文章)的维护 owner 和更新机制**:谁维护、多久更新、有没有变更通知?(同步机制设计的前提)
7. **拿典型案例现场对齐**:带上"WOM 任务令"案例、"查不到销售订单无 URL"案例、"帮忙看下"案例,让专员现场讲当时是怎么判断的——你要的不是这三条的答案,是他们脑子里的判断规则。

---

**基于卡点修正后的动作序列**:KT 去重你已完成 → 现在**直接写抽取脚本**(用上面那个带"不确定"的宽 schema,LLM 批量抽 + 你抽查 20 条校对)→ 产出**权限项去重清单**和**映射表 v0(含空缺标记)**→ 统计 Description 含链接率、页面缺失率、C 层占比三个数字 → 带着清单、映射表样例、三个数字和上面 7 个问题去开会。这样会上你不是"来请教的",是"带着数据资产来要确认的",会议效率完全不同。