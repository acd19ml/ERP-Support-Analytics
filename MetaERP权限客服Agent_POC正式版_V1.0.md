# MetaERP 权限客服 Agent POC 正式方案

> **文档版本：** V1.0  
> **文档状态：** POC 正式方案  
> **适用范围：** MetaERP 已有工单中的权限专题  
> **目标读者：** 产品、客服、权限平台、架构、研发、测试及相关业务 Owner  
> **核心目标：** 验证 Agent 能否在安全、可解释、可评测的前提下，独立闭环一部分高频权限类客服问题，降低人工客服介入量

---

## 一、执行摘要

MetaERP 当前每月约有 3000 条以上工单，其中权限相关工单约 200～300 条。权限问题虽然高频，但处理过程并不是简单的 FAQ 匹配，而是涉及用户诉求理解、页面与功能定位、角色与数据维度识别、用户实时权限查询、资格校验、规则解释以及必要时转人工。

本 POC 的核心判断是：

> **不能把历史工单四列表格或相似工单检索直接当作 Agent 的决策能力。**

现有“子产品、问题表象、解决措施、来源部门”类表格适合做趋势分析和 FAQ 汇总，但不足以支持 Agent 给出准确、安全、可执行的权限建议。正式方案需要将以下能力组合起来：

1. **在线问题理解与专题路由**：识别是否进入权限客服流程，并判断用户当前遇到的表象；
2. **问题模式知识**：描述同一表象下的候选原因、验证条件、追问方式和解决动作；
3. **角色与权限权威知识**：提供角色能力、数据维度、申请资格、审批与生效规则；
4. **用户运行时权限查询**：获取用户当前角色、有效期、数据维度、活动角色及所属权限体系；
5. **Agent 对话与诊断流程**：在信息不足时追问，在证据充分时给出动作，在无法可靠判断时转人工；
6. **评测与反馈闭环**：通过历史回放和线上指标验证是否真正降低人工工单，而不是只验证回答是否“看起来合理”。

POC 第一阶段建议聚焦：

```text
专题 = 权限专题
L1 = 7-查不到数据
L2 = null
```

但不能预先只选“缺数据维度”或“权限规则解释”案例，而应覆盖该问题表象下的全部真实解决分支，包括缺角色、缺数据维度、角色切换、权限过期、查询条件、业务数据异常、后台修复及人工介入。

---

## 二、项目背景

人工客服处理权限问题时，通常需要结合以下信息：

- 用户的问题描述；
- 用户所在岗位、部门、组织或 BG；
- 用户当前拥有的角色；
- 角色状态及有效期；
- 角色下配置的数据维度；
- 当前激活或切换的角色；
- 用户访问的系统、页面、按钮或业务对象；
- 权限申请规则与岗位资格；
- 历史处理经验；
- 必要时通过追问、拉群、电话或跨团队协同补充信息。

典型诉求包括：

- 某个页面或菜单无法访问；
- 某个按钮不可用或提示无权限；
- 某类业务数据查询不到；
- 不知道应该申请哪个角色；
- 已有角色但仍看不到目标组织的数据；
- 权限过期、角色切换、跨 BG 或涉密权限问题；
- 不理解角色、数据维度或权限生效逻辑；
- 希望后台直接帮助配置或修复权限。

因此，本项目不是“把历史工单整理成一份 Agent 指南”，而是：

> **建立一套能够支撑 Agent 完成权限客服判断的知识、权威事实、运行时查询能力、对话决策机制和评测体系，并通过高频问题 POC 验证实际解决效果。**

---

## 三、项目目标与非目标

### 3.1 业务目标

POC 上线后，重点观察以下业务结果：

- 权限类人工工单数量是否下降；
- Agent 独立闭环的权限问题数量；
- 用户确认解决的比例；
- 转人工比例；
- 同一问题短期重复开单比例；
- 平均解决时长变化；
- 用户满意度变化。

POC 不以“回答覆盖率”作为唯一成功标准。对于无法可靠判断的问题，正确追问或及时转人工同样属于有效结果。

### 3.2 技术目标

POC 需要验证：

1. Agent 能否正确识别权限专题及用户问题表象；
2. Agent 能否识别当前信息是否充分；
3. 信息不足时，能否提出有效且最少必要的追问；
4. Agent 能否生成并排查缺角色、缺维度、权限过期、角色切换等候选原因；
5. Agent 能否调用权限查询能力，结合用户真实状态排除错误原因；
6. Agent 能否给出精确、可执行、可验证的解决动作；
7. Agent 能否区分权限问题、业务数据问题、缺陷、需求及其他 IT 专题；
8. Agent 能否在证据不足或高风险场景下停止猜测并转人工；
9. 历史工单、角色目录、规则知识和权限 API 各自能够解决哪些问题。

### 3.3 第一阶段非目标

POC 第一阶段暂不追求：

- 覆盖全部权限工单；
- 建设完整企业级权限知识图谱；
- 自动执行高风险权限变更；
- 自动处理全部跨系统、跨 BG 和涉密权限；
- 仅从历史工单自动推导岗位申请资格；
- 建设适用于全部 ERP 客服问题的通用 Agent；
- 让大模型自行完成确定性的权限计算；
- 完全替代人工客服；
- 一开始即确定必须使用 Wiki、向量库、知识图谱或某一种 Agent 框架。

技术形态应根据实际诊断任务、数据条件和 POC 回放结果确定。

---

## 四、分类体系及使用原则

现有分类体系包含：

```text
第一维：专题
第二维：问题表象 L1 / L2
第三维：历史解决方案 S1～S7
辅助字段：边界
```

三维并非对所有工单同时生效。

### 4.1 专题是第一层路由和停止条件

分类首先判断以下五类 IT 或外部专题：

1. 外部/颗粒；
2. IT 运维/基础设施；
3. IT 开发/测试问题；
4. IT 缺陷/性能问题；
5. IT 需求。

一旦确认命中：

```text
专题 = 对应专题
L1 = null
L2 = null
解决方案分类 = null
```

只有未命中上述终止专题的工单，才继续进入业务类判断：

```text
权限专题
或
其他业务
```

因此，专题不是普通标签，而是 Agent 是否进入权限诊断流程的第一层路由。

### 4.2 L1/L2 描述用户表象，不描述根因

业务类工单继续判断 L1：

| L1 | 含义 |
| --- | --- |
| 1 | 找不到页面入口或功能 |
| 2 | 系统使用咨询 |
| 3 | 操作失败或操作不生效 |
| 4 | 业务数据异常或原因定位 |
| 5 | 帮助查询或确认数据 |
| 6 | 帮助维护配置或操作 |
| 7 | 查不到数据 |
| 8 | 系统逻辑或规则咨询 |

只有以下 L1 继续判断 L2：

```text
L1 = 2 → 2a / 2b
L1 = 3 → 3a / 3b / 3c
L1 = 8 → 8a / 8b / 8c
```

其他 L1 的 L2 必须为 `null`。

例如：

```text
权限专题 + L1=7-查不到数据 + L2=null
```

不能自行扩展为“7a 缺角色”“7b 缺维度”。缺角色、缺维度是后续诊断原因，不属于当前分类体系。

同一个权限原因可能表现为不同 L1：

| 用户表象 | 分类 |
| --- | --- |
| 不知道该申请哪个角色 | 2b-如何申请权限 |
| 操作时提示无权限 | 3a-异常报错 |
| 页面可以进入但查不到数据 | 7-查不到数据 |
| 询问多个维度如何取交集 | 8a-规则/逻辑咨询 |
| 请求后台直接提权 | 6-帮助维护配置/操作 |

因此不能从 `L1=7` 直接推导“缺数据维度”。

### 4.3 S1～S7 是历史解决结果，不是 Agent 运行时动作

历史解决方案分类包括：

| 分类 | 含义 |
| --- | --- |
| S1 | 申请功能权限，即角色 |
| S2 | 申请数据权限，即数据维度 |
| S3 | 操作指引 |
| S4a | 澄清业务规则 |
| S4b | 诊断数据异常 |
| S4c | 解释权限规则 |
| S5 | 数据查询 |
| S6 | 后台配置或修复 |
| S7 | 人工介入 |

S 分类回答的是：

> 这张历史工单最终采用了哪一类关键解决方式？

它不等于 Agent 下一步动作。Agent 运行时动作还包括：

- 追问用户；
- 检索术语；
- 定位产品与功能；
- 查询用户权限；
- 查询角色目录；
- 比较权限差异；
- 请求人工核实；
- 等待用户确认；
- 再次诊断。

建议在数据结构中明确区分：

```json
{
  "historical_solution_class": "S2-申请数据权限（数据维度）",
  "runtime_actions": [
    "ASK_USER",
    "QUERY_USER_PERMISSION",
    "CHECK_DATA_SCOPE",
    "RECOMMEND_DIMENSION_APPLICATION"
  ]
}
```

### 4.4 离线分类器与在线问题理解器必须分离

现有 `classify.py` 输入包含：

```text
问题标题 + 问题描述 + 解决方案
```

它适合处理已完成的历史工单，用于：

- 历史数据整理；
- 问题分布分析；
- 知识候选发现；
- 训练集与评测集建设。

新工单进入 Agent 时不存在“解决方案”字段，因此不能直接复用同一输入逻辑，否则会产生目标信息泄漏。

#### 离线历史分类器

输入：

```text
标题 + 问题描述 + 已知解决方案
```

输出：

```text
专题 + L1 + L2 + S 分类 + 边界
```

#### 在线问题理解器

输入：

```text
用户当前问题 + 对话上下文 + 当前可获得的系统信息
```

初始输出：

```text
初步专题
初步 L1
适用时的初步 L2
边界或歧义信号
已识别实体
缺失信息
候选解决路径
```

在线入口阶段不能直接输出确定的 S 分类。最终 S 分类应在诊断完成后，用于记录本次问题实际采用的核心解决方式。

### 4.5 在线缺陷和需求判断需谨慎

用户出现报错、加载慢或查询超时，不等于已经确认系统缺陷。在线 Agent 最多可判断为“疑似缺陷或性能问题”，并通过以下方式核实：

- 查询已知缺陷库；
- 查询是否存在相同报错及已确认 DTS；
- 调用监控或健康检查；
- 转研发或人工确认。

同样需要区分：

```text
询问当前是否支持某能力 → L1=8，通常为 8b
提出希望新增某能力 → 专题=IT需求
```

---

## 五、已有数据与资产

### 5.1 工单字段

现有工单主要包含：

- 事件单 ID；
- 用户 ID；
- 部门；
- 产品；
- 子产品；
- 应用系统模块；
- 应用 CI；
- 地点；
- 来源；
- 问题标题；
- 问题描述；
- 解决方案；
- 问题描述中的 URL；
- 服务分类及子分类；
- 事件分类；
- 源业务系统 URL；
- 来源系统分类；
- 关联工单和关联 PM 单；
- 开单人、开单时间、处理人、解决人；
- 服务评价；
- 变更号；
- 其他标签及领域标签。

并非所有字段都会直接进入 Agent 知识。需要根据用途区分：

- 问题理解字段；
- 功能定位字段；
- 用户与组织上下文字段；
- 历史解决结果字段；
- 评测与追溯字段。

### 5.2 已有分类资产

现有分类结果可用于：

- 工单路由；
- 问题模式候选发现；
- 检索过滤；
- 历史结果分布分析；
- 离线抽样；
- Agent 问题理解测试；
- 边界案例建设。

但分类结果不能直接回答：

- 应该申请哪个具体角色；
- 应该补充哪个具体数据维度；
- 用户是否已拥有相同或等价权限；
- 用户是否具备申请资格；
- 当前问题是否由业务数据异常导致。

### 5.3 已有人工标注

少量人工准确裁决的专题、L1、L2、S 分类和边界数据，可用于：

- 评估和迭代历史分类器；
- 构建在线问题理解测试集；
- 形成高置信 few-shot 示例；
- 选择典型和边界案例；
- 验证 Agent 是否进入正确处理路径。

现有标注仍不足以评价：

- 推荐角色是否正确；
- 推荐维度是否正确；
- 追问是否充分；
- 资格与安全校验是否正确；
- 最终方案是否真正解决问题。

---

## 六、Agent 权限问题决策链

Agent 运行过程不应被简化为：

```text
用户问题 → 检索相似工单 → 输出历史解决方案
```

正式决策链应包括以下阶段。

### 6.1 专题路由

判断问题是否属于：

- 外部/颗粒；
- IT 运维/基础设施；
- IT 开发/测试；
- 已确认或疑似缺陷/性能；
- IT 需求；
- 权限专题；
- 其他业务。

非权限问题应进入对应流程，不继续执行权限诊断。

### 6.2 理解用户问题表象

识别：

- L1 及适用时的 L2；
- 用户是否明确提到权限；
- 涉及哪个产品或子产品；
- 用户已经执行了什么操作；
- 是否存在明确报错；
- 当前描述中有哪些关键实体；
- 当前信息是否足以进入诊断。

### 6.3 定位功能和业务上下文

尽量明确：

- 系统；
- 产品；
- 子产品；
- 页面；
- 菜单；
- 按钮；
- 功能；
- 资源点；
- 业务对象；
- 目标组织或数据范围。

URL 是强定位证据之一，但不是所有问题的必填项。还可使用：

- 页面或菜单名称；
- 按钮名称；
- 报错原文；
- 截图；
- 业务对象；
- 用户岗位；
- 历史相似表达；
- 当前对话上下文。

每个问题模式应定义：

- 最低必需上下文；
- 可选定位信息；
- 无法唯一定位时的追问；
- 停止判断并转人工的条件。

### 6.4 生成候选原因

同一表象可能对应多个真实原因。

例如“查不到数据”可能是：

1. 缺少访问功能所需角色；
2. 已有角色，但缺少目标数据维度；
3. 角色或维度权限已过期；
4. 当前激活或切换到了错误角色；
5. 目标数据属于另一 BG；
6. 涉及涉密组织或特殊限制；
7. 查询条件不完整；
8. 业务数据尚未生成；
9. 系统同步延迟；
10. 后台配置异常；
11. 系统缺陷；
12. 当前知识和工具无法覆盖。

知识结构必须表达为：

```text
问题表象
→ 需要补充的上下文
→ 候选原因
→ 每个原因的验证条件
→ 查询或追问方式
→ 验证结果对应的解决动作
```

### 6.5 查询用户真实状态

Agent 需要通过系统工具获取：

- 用户当前角色；
- 角色状态；
- 角色生效和失效时间；
- 每个角色的数据维度；
- 当前活动角色；
- 所属 BG 或权限体系；
- 是否存在自动激活角色；
- 是否已拥有等价权限。

这部分属于系统权威事实，不能由模型根据历史工单推测。

### 6.6 查询角色和权限规则

需要确认：

- 目标功能对应的候选角色；
- 角色允许访问的功能范围；
- 角色需要的数据维度；
- 角色是否自动激活；
- 申请入口；
- 审批规则；
- 生效时间；
- 申请资格；
- 互斥角色；
- 跨 BG、涉密或高风险限制；
- 角色之间的区别及常见混淆项。

### 6.7 资格和安全校验

即使确认用户缺少某角色，也不能立即建议申请。还需检查：

- 用户是否具备正式申请资格；
- 是否需要审批；
- 是否为自动激活角色；
- 是否存在互斥或冲突权限；
- 是否允许跨 BG 申请；
- 是否涉及涉密或高风险权限；
- 是否已有等价角色；
- 推荐动作是否可能造成越权。

岗位与角色之间的正式资格规则必须来自权限系统、角色 Owner、管理平台或制度文档，不能仅从工单统计推导。

### 6.8 生成解决动作

最终输出至少应包括：

1. 当前判断；
2. 判断依据；
3. 已核实的用户权限状态；
4. 需要申请、补充、切换或操作的精确对象；
5. 申请或操作入口；
6. 预计生效时间；
7. 生效后的验证方法；
8. 当前判断的不确定性；
9. 仍无法确认时的下一步或转人工说明。

---

## 七、总体技术方案

建议采用“在线理解、问题模式、权威知识、权限查询、确定性校验、Agent 编排”组合架构。

```text
用户 / 工单入口
      ↓
专题路由器
      ↓
在线问题理解器
      ↓
问题模式检索与上下文完整性检查
      ↓
追问 / 功能定位
      ↓
候选原因生成
      ↓
用户权限查询 + 角色目录 + 规则知识
      ↓
确定性差异比较与安全校验
      ↓
解决动作生成
      ↓
用户确认 / 再次诊断 / 转人工
      ↓
记录实际解决结果与 S 分类
```

### 7.1 专题路由器

职责：

- 区分五类终止专题和业务类；
- 防止权限 Agent 处理缺陷、需求、测试或基础设施问题；
- 对疑似缺陷保留不确定性；
- 将非权限范围问题转入相应流程。

建议输出：

```json
{
  "topic": "权限专题",
  "is_terminal_topic": false,
  "confidence": 0.91,
  "ambiguities": []
}
```

### 7.2 在线问题理解器

职责：

- 判断 L1；
- 仅在 L1=2/3/8 时判断 L2；
- 抽取产品、页面、URL、报错、角色、维度、业务对象和目标组织；
- 判断上下文是否充分；
- 输出缺失信息和候选解决路径；
- 不提前输出最终 S 分类。

### 7.3 问题模式库

每个问题模式定义：

- 适用专题、L1/L2、产品和功能范围；
- 用户典型表达；
- 最低必需上下文；
- 可选定位信息；
- 追问问题；
- 候选原因；
- 每个原因的验证条件；
- 所需工具；
- 可能的解决动作；
- 转人工条件；
- 支持案例与审核状态。

### 7.4 角色目录与规则知识

角色目录提供：

- 角色标准名称、展示名称及别名；
- 所属系统、产品和子产品；
- 包含与不包含的能力；
- 对应功能和资源；
- 必需数据维度；
- 典型适用岗位；
- 正式申请资格；
- 审批和激活方式；
- 申请入口；
- 生效时间；
- 角色 Owner；
- 权威来源；
- 最后核实时间；
- 字段级可信度。

### 7.5 权限查询与诊断工具

最小能力包括：

- 查询用户当前角色；
- 查询角色状态与有效期；
- 查询角色下的数据维度；
- 查询当前活动角色；
- 查询所属 BG 或权限体系；
- 查询功能所需角色与维度；
- 比较用户状态与目标访问条件。

长期可由权限系统提供权威差异诊断，模型只负责解释结果。

### 7.6 Agent Runtime

职责：

- 管理对话状态；
- 选择追问；
- 选择问题模式；
- 调用工具；
- 基于证据排除候选原因；
- 生成可解释答案；
- 处理工具不可用和冲突信息；
- 触发转人工；
- 记录完整诊断轨迹。

---

## 八、知识与数据分层

### 8.1 客户表达和术语知识

用于解决：

- ERP 缩写难以理解；
- 用户使用口语、简称或别名；
- 页面、功能和角色存在多种叫法；
- 不同部门对同一概念表达不同。

内容包括：

- 产品、子产品名称；
- 页面、菜单、功能名称；
- 角色名及别名；
- 数据维度名称；
- 缩写；
- 常见用户表达；
- 报错原文；
- 同义词和易混淆词。

主要来源为历史工单和人工客服经验。

### 8.2 问题模式和客服经验知识

用于回答：

- 某种表象通常有哪些候选原因；
- 还缺少什么信息；
- 应优先检查什么；
- 应该如何追问；
- 什么情况下必须转人工。

来源包括：

- 历史工单；
- 专业客服经验；
- 人工裁决；
- 高频案例回放；
- Agent 运行反馈。

### 8.3 权限权威事实

用于回答：

- 某功能对应哪些角色；
- 某角色允许访问哪些功能；
- 某角色需要哪些数据维度；
- 某角色是否自动激活；
- 哪些岗位有资格申请；
- 申请入口、审批和生效规则；
- 角色之间的区别。

来源必须是权限系统、角色 Owner、管理平台或正式制度。

### 8.4 用户运行时状态

用于回答：

- 用户当前拥有哪些角色；
- 当前角色是否有效；
- 已配置哪些数据维度；
- 权限是否过期；
- 当前使用哪个角色；
- 是否属于目标 BG；
- 是否已经具有等价权限。

应由实时 API 或查询工具提供。

### 8.5 对话与决策逻辑

用于决定：

- 信息不足时问什么；
- 多个候选原因如何排查；
- 何时停止追问；
- 何时不能继续猜测；
- 何时转人工；
- 如何向用户解释判断依据。

可由状态机、规则、问题模式、LLM 推理和工具调用共同实现。

---

## 九、历史工单的使用边界

### 9.1 可直接沉淀

- 用户原始表述；
- 页面和功能名称；
- URL；
- 报错原文；
- 明确出现的角色名；
- 明确出现的数据维度；
- 某一工单最终采用的处理动作；
- 是否转人工；
- 用户常见问题模式；
- 角色和术语别名；
- 已验证解决案例。

### 9.2 可作为候选线索

- 某页面可能对应某角色；
- 某类问题可能与某维度有关；
- 某角色可能存在资格限制；
- 某问题可能经常涉及跨 BG；
- 某页面可能经常发生角色切换问题。

候选线索必须经过系统或人工核实，才能进入正式决策知识。

### 9.3 不能直接推导

- 角色的完整功能边界；
- 某页面永远只需要某一个角色；
- 所有具备角色申请资格的岗位；
- 完整岗位—角色映射；
- 正式审批规则；
- 完整资源点—功能点—角色映射；
- 未在工单中出现过的合法角色；
- 当前用户实时权限状态。

---

## 十、问题模式数据结构

问题模式建议采用以下核心结构：

```json
{
  "pattern_id": "PATTERN_007",
  "name": "已进入页面但查不到目标组织数据",
  "scope": {
    "topic": "权限专题",
    "l1": "7-查不到数据",
    "l2": null,
    "products": [],
    "sub_products": [],
    "functions": []
  },
  "symptom_patterns": {
    "canonical_description": "用户已经进入查询页面，但无法看到目标组织或公司的数据。",
    "example_utterances": [
      "查不到广州公司的数据",
      "只能看到深圳，看不到广州",
      "这个组织的数据搜不到"
    ],
    "error_messages": []
  },
  "minimum_context": [
    "target_business_object",
    "target_dimension"
  ],
  "disambiguation_context": [
    "product",
    "sub_product",
    "page_name",
    "url",
    "query_conditions"
  ],
  "follow_up_questions": [
    "您查询的是哪类业务数据？",
    "需要查看的是哪个公司或组织？",
    "当前页面名称或链接是什么？",
    "页面是否有报错提示？"
  ],
  "candidate_causes": [
    {
      "cause_code": "MISSING_ROLE",
      "checks": [
        "定位目标功能对应的候选角色",
        "查询用户是否拥有有效角色"
      ],
      "possible_resolutions": [
        {
          "resolution_code": "APPLY_ROLE",
          "historical_solution_class": "S1-申请功能权限（角色）"
        }
      ]
    },
    {
      "cause_code": "MISSING_DATA_SCOPE",
      "checks": [
        "查询用户已有角色",
        "查询角色下的数据维度",
        "比较目标组织与已有维度"
      ],
      "possible_resolutions": [
        {
          "resolution_code": "APPLY_DATA_SCOPE",
          "historical_solution_class": "S2-申请数据权限（数据维度）"
        }
      ]
    },
    {
      "cause_code": "WRONG_ACTIVE_ROLE",
      "checks": [
        "确认当前激活角色是否正确"
      ],
      "possible_resolutions": [
        {
          "resolution_code": "SWITCH_ACTIVE_ROLE",
          "historical_solution_class": "S4c-解释权限规则"
        }
      ]
    },
    {
      "cause_code": "QUERY_CONDITION_INCOMPLETE",
      "checks": [
        "检查查询条件是否覆盖目标范围"
      ],
      "possible_resolutions": [
        {
          "resolution_code": "GUIDE_QUERY_OPERATION",
          "historical_solution_class": "S3-操作指引"
        }
      ]
    },
    {
      "cause_code": "BUSINESS_DATA_ABNORMAL",
      "checks": [
        "确认目标业务数据是否已生成",
        "检查数据同步或业务状态"
      ],
      "possible_resolutions": [
        {
          "resolution_code": "DIAGNOSE_DATA",
          "historical_solution_class": "S4b-诊断数据异常"
        }
      ]
    },
    {
      "cause_code": "UNKNOWN_OR_COMPLEX",
      "checks": [],
      "possible_resolutions": [
        {
          "resolution_code": "ESCALATE",
          "historical_solution_class": "S7-人工介入"
        }
      ]
    }
  ],
  "escalation_conditions": [
    "无法唯一定位目标功能",
    "候选原因之间无法排除",
    "权限状态与实际现象冲突",
    "涉及涉密或高风险权限",
    "角色资格规则未核实",
    "关键查询工具不可用"
  ],
  "evidence": {
    "incident_ids": [],
    "support_count": 0,
    "review_status": "candidate",
    "last_verified_at": null
  }
}
```

---

## 十一、角色目录设计

### 11.1 建设来源

角色目录应综合：

1. 权限系统配置；
2. 资源点—功能点—角色映射；
3. 角色 Owner 提供的信息；
4. 正式制度和岗位资格规则；
5. 工单中出现的角色名与别名；
6. 人工客服经验；
7. Agent 运行中出现的新角色。

### 11.2 最小 Schema

```json
{
  "role_id": "ROLE_001",
  "role_name": "General_Accountant",
  "display_name": "总账会计",
  "aliases": [
    "GA",
    "总账角色"
  ],
  "system": "MetaERP",
  "product": "财经",
  "sub_product": "总账",
  "description": {
    "purpose": "用于执行总账查询和相关会计处理。",
    "included_capabilities": [
      "总账凭证查询"
    ],
    "excluded_capabilities": [
      "不自动授予所有公司数据权限"
    ]
  },
  "resources": [
    {
      "resource_id": "RES_001",
      "resource_name": "总账凭证查询页面"
    }
  ],
  "required_dimensions": [
    {
      "dimension_type": "company",
      "required": true,
      "description": "决定可查询的公司范围"
    }
  ],
  "eligibility": {
    "allowed_departments": [],
    "allowed_job_families": [],
    "requires_approval": true
  },
  "activation": {
    "mode": "application",
    "application_entry": null,
    "expected_effective_time": null
  },
  "owner": {
    "department": null,
    "contact": null
  },
  "knowledge_status": {
    "verification_status": "unverified",
    "authoritative_source": null,
    "last_verified_at": null
  }
}
```

### 11.3 可信度分级

建议至少定义：

- `system_verified`：系统配置读取；
- `owner_verified`：角色 Owner 已确认；
- `policy_verified`：正式制度文档；
- `reviewed`：人工审核；
- `multi_case_supported`：多个工单支持；
- `single_case_hint`：单个工单线索；
- `unverified`：未核实；
- `conflict`：来源冲突；
- `deprecated`：已失效。

申请资格、岗位限制、跨 BG、涉密及高风险字段，不得由未核实工单线索直接驱动确定性建议。

---

## 十二、权限 API 能力设计

### 12.1 用户当前授权查询

输入：

```json
{
  "user_id": "U123"
}
```

建议输出：

```json
{
  "user_id": "U123",
  "organization": {
    "department": "某部门",
    "bg": "某BG",
    "job": "某岗位"
  },
  "current_context": {
    "active_role_id": "ROLE_001"
  },
  "roles": [
    {
      "role_id": "ROLE_001",
      "role_name": "General_Accountant",
      "status": "active",
      "valid_from": "2026-01-01",
      "valid_to": "2026-12-31",
      "dimensions": [
        {
          "dimension_type": "company",
          "dimension_code": "CN_GZ",
          "dimension_name": "广州公司"
        }
      ]
    }
  ],
  "data_freshness": "2026-07-30T16:00:00+08:00",
  "limitations": []
}
```

### 12.2 功能所需权限查询

输入：

```json
{
  "system": "MetaERP",
  "product": "某产品",
  "function_id": "FUNCTION_001"
}
```

建议输出：

```json
{
  "function_id": "FUNCTION_001",
  "candidate_roles": [
    {
      "role_id": "ROLE_001",
      "relation": "required"
    }
  ],
  "required_dimensions": [
    {
      "dimension_type": "company",
      "required": true
    }
  ],
  "source": "permission_configuration",
  "last_verified_at": "2026-07-01"
}
```

### 12.3 权限差异诊断

长期目标由权限系统提供确定性访问诊断。

输入：

```json
{
  "user_id": "U123",
  "resource_id": "RESOURCE_001",
  "target_context": {
    "company": "CN_GZ"
  }
}
```

输出：

```json
{
  "access": false,
  "reason_codes": [
    "MISSING_DATA_SCOPE"
  ],
  "missing": [
    {
      "type": "dimension",
      "dimension_type": "company",
      "dimension_code": "CN_GZ"
    }
  ],
  "authoritative": true
}
```

### 12.4 API 局限

即使接入权限 API，也不能解决全部问题：

- 用户口语可能无法准确映射到资源点；
- 页面、按钮和后端资源点可能不是一一对应；
- 权限正确不代表业务数据一定存在；
- 数据可能尚未同步；
- 用户可能选择了错误查询条件；
- 跨系统权限数据可能不一致；
- API 数据可能存在更新延迟；
- 部分规则可能仍只存在于人工流程。

因此，权限 API 是诊断工具，不是完整知识库的替代品。

---

## 十三、知识生产与审核流程

### 13.1 原始案例保留

每条工单作为可追溯案例保留：

- 用户原始问题；
- 产品和子产品；
- 页面、URL 和报错；
- 分类结果；
- 解决方案原文；
- 明确角色和维度；
- 是否转人工；
- 最终结果；
- 来源工单 ID。

### 13.2 结构化信息抽取

模型可抽取：

- 用户典型表达；
- 页面和功能；
- 角色名；
- 数据维度；
- 报错；
- 已执行操作；
- 工单中明确说明的原因；
- 最终动作；
- 仍缺失的信息。

抽取规则：

- 禁止改写精确角色名；
- 禁止补全工单未写的信息；
- 禁止把单个案例直接升级为通用规则；
- 每个字段保留来源；
- 无法确认时填写空值或未知；
- 区分原文事实与模型归纳。

### 13.3 模式聚类

不能仅按：

```text
子产品 × L1 × S 分类
```

直接生成最终问题模式。

建议过程：

```text
第一层：专题 + 产品/子产品 + L1/L2 + 功能范围
第二层：观察该表象下不同 S 分类分布
第三层：抽取并区分不同真实原因
第四层：形成“原因—检查—动作”分支
```

S 分类适合用于：

- 描述历史结果分布；
- 发现可能的解决分支；
- 分层抽样；
- 评估问题模式是否遗漏解决路径。

### 13.4 边界案例处理

| 数据类型 | 用途 |
| --- | --- |
| 边界=false 且人工确认 | 首批知识种子、基础评测集 |
| 边界=true 且人工裁决 | 歧义处理、追问设计、困难评测集 |
| 边界=true 且未裁决 | 不进入正式知识，进入待审核队列 |

边界案例不能全部丢弃，因为它们通常包含 Agent 最需要掌握的区分能力。

### 13.5 候选知识与正式知识分离

建议状态：

- `candidate`：少量工单支持，尚未核实；
- `reviewed`：经客服或业务人工审核；
- `verified`：由系统、角色 Owner 或正式规则确认；
- `conflict`：来源之间存在冲突；
- `deprecated`：规则已失效。

支持工单数为 1 且无其他证据的条目，不得直接进入正式自动决策。

---

## 十四、Agent 运行时流程

```text
用户提出问题
    ↓
在线专题初判
    ├── 外部/颗粒 → 转对应方或人工
    ├── IT运维/基础设施 → 转运维流程
    ├── IT开发/测试 → 转开发测试流程
    ├── 已确认或疑似缺陷/性能 → 查询缺陷或转研发
    ├── IT需求 → 进入需求收集流程
    └── 业务类
           ↓
       判断权限专题 / 其他业务
           ↓
       判断 L1
           ↓
       仅 L1=2/3/8 时判断 L2
           ↓
       抽取页面、功能、角色、维度、报错等实体
           ↓
       检查上下文是否充分
           ├── 不充分：针对性追问
           └── 充分：匹配问题模式
                         ↓
                  生成候选原因
                         ↓
                  查询用户实际权限
                         ↓
                  查询角色与规则知识
                         ↓
                  执行确定性比较与安全校验
                         ↓
                  排除或确认候选原因
                         ↓
                  生成解决动作
                         ↓
                  用户确认
              ├── 已解决：记录结果和实际 S 分类
              ├── 未解决：再次诊断
              └── 无法可靠处理：转人工
```

任何阶段都允许以下结束状态：

- 已解决；
- 需要用户补充信息；
- 需要人工客服；
- 需要角色 Owner 或其他部门确认；
- 系统查询工具不可用；
- 当前知识未覆盖；
- 疑似缺陷或业务数据问题，转对应流程。

---

## 十五、Agent 输出规范

Agent 不应只回答：

> 请申请相应权限。

建议输出结构如下：

```text
当前判断：
您已经可以进入页面，但无法看到广州公司的目标数据。根据现有信息，问题更可能与数据权限范围有关，而不是页面功能权限。

核实结果：
您当前拥有角色 General_Accountant，角色状态有效；但该角色下暂未查询到广州公司的公司维度。

建议动作：
请在权限申请入口中，为角色 General_Accountant 补充广州公司对应的公司维度权限。

验证方式：
权限生效后，请重新进入原页面，并使用相同查询条件检查广州公司的数据。

当前限制：
若补充维度后仍无数据，可能涉及业务数据尚未生成、跨 BG、同步延迟或查询条件问题，需要进一步排查。
```

证据不足时应明确说明：

```text
目前还不能判断您缺少哪个权限。需要先确认页面名称、目标数据对象和报错信息；在这些信息不完整时直接推荐角色可能导致错误申请。
```

转人工时应同步生成结构化上下文，包括：

- 用户原始问题；
- 已识别专题与 L1/L2；
- 已补充信息；
- 已查询的用户权限；
- 已排除原因；
- 仍存疑点；
- 建议人工下一步检查项。

---

## 十六、POC 样本选择

### 16.1 首个问题族

建议选择：

```text
专题 = 权限专题
L1 = 7-查不到数据
L2 = null
```

不提前只过滤 S2 或 S4c，而是观察全部真实 S 分布：

| 历史 S 分类 | 代表含义 |
| --- | --- |
| S1 | 缺功能角色 |
| S2 | 缺数据维度 |
| S3 | 查询或操作问题 |
| S4a | 业务规则解释 |
| S4b | 实际是业务数据异常 |
| S4c | 角色切换、过期或权限逻辑 |
| S5 | 需要人工代查数据 |
| S6 | 后台配置或修复 |
| S7 | 无法自动解决 |

实际是否存在各类别，应先统计，不预设分布。

### 16.2 分层抽样

#### 基础组

```text
边界=false
问题描述较充分
解决结果明确
```

用于验证理想条件下的解决能力。

#### 歧义组

```text
边界=true
且已完成人工裁决
```

用于验证追问和区分能力。

#### 不可自动解决组

```text
最终为 S7
或关键事实无法获得
或涉及高风险权限
```

用于验证 Agent 是否及时转人工，而不是强行作答。

### 16.3 历史回放要求

历史回放时：

提供给 Agent：

```text
标题 + 问题描述 + 当时可获得的用户与系统信息
```

隐藏：

```text
解决方案 + 最终原因 + 人工处理结果
```

验证 Agent 是否能：

- 提出正确追问；
- 调用正确查询；
- 识别正确原因；
- 给出正确动作；
- 在必要时转人工。

否则只能证明模型会复述历史答案，不能证明具备诊断能力。

---

## 十七、评测体系

### 17.1 业务指标

- 权限类人工工单下降比例；
- Agent 独立解决率；
- 用户确认解决率；
- 转人工率；
- 短期重复开单率；
- 平均解决时长；
- 用户满意度。

### 17.2 历史分类器评测

允许使用历史解决方案字段，评价：

- 专题准确率；
- L1 准确率；
- L2 准确率；
- S 分类准确率；
- 边界识别效果。

### 17.3 在线问题理解评测

不提供历史解决方案，评价：

- 是否正确识别终止专题；
- 是否正确进入业务类；
- 是否正确识别权限专题；
- L1 是否正确；
- 适用时 L2 是否正确；
- 是否识别信息不足；
- 是否提出必要追问；
- 是否提出无关追问；
- 是否错误提前判断最终解决方案。

### 17.4 端到端诊断评测

- 候选原因覆盖率；
- 最终原因准确率；
- 缺角色识别准确率；
- 缺维度识别准确率；
- 权限过期识别准确率；
- 角色切换识别准确率；
- 推荐角色准确率；
- 推荐维度准确率；
- 是否重复推荐已有权限；
- 是否误把数据异常当作权限问题；
- 是否错误将需求或缺陷当作权限问题；
- 是否在应转人工时及时转人工；
- 用户是否实际解决问题。

### 17.5 安全指标

- 不必要申请率；
- 已有角色重复申请率；
- 不符合资格的推荐率；
- 越权建议率；
- 涉密或高风险权限误推荐率；
- 应转人工但未转人工比例；
- 将非权威知识当作确定事实的比例；
- 工具查询失败后仍输出确定结论的比例。

### 17.6 端到端金标准

每条案例建议标注：

- 用户原始问题；
- 正确专题、L1、L2；
- 已知上下文；
- 必须追问的问题；
- 最终原因；
- 所需角色；
- 所需数据维度；
- 是否需要申请；
- 正确解决动作；
- 不应采取的动作；
- 是否需要转人工；
- 最终是否解决。

---

## 十八、分阶段实施计划

### 阶段一：单问题族人工原型

**目标：**

- 验证问题模式结构；
- 验证最低必要上下文；
- 明确权限 API 最小字段；
- 建立端到端评测样本；
- 明确自动解决边界。

**建议工作：**

1. 筛选 `权限专题 + L1=7` 工单；
2. 统计全部 S 分类和边界分布；
3. 选取 20～50 条代表案例；
4. 人工确认真实原因与最终动作；
5. 手工编写第一个完整问题模式；
6. 明确所需用户状态字段；
7. 调研现有系统是否可提供；
8. 隐藏解决方案进行历史回放；
9. 统计失败原因。

**交付物：**

- 一个完整问题模式；
- 20～50 条历史回放案例；
- 初版追问策略；
- 初版权限查询契约；
- 涉及角色的人工确认信息；
- 失败原因分类与统计；
- POC 自动解决与转人工边界。

### 阶段二：高频模式 POC

**目标：**

- 覆盖 10～20 个高频权限问题模式；
- 支持用户权限实时查询；
- 建立角色目录 V0.1；
- 支持有限范围自动解答。

**交付物：**

- 问题模式库；
- 术语库；
- 角色目录 V0.1；
- 用户权限查询 API；
- Agent 对话和诊断流程；
- 离线评测集；
- 转人工流程；
- 运行日志和指标看板。

### 阶段三：扩大覆盖与权威能力建设

**目标：**

- 补齐角色目录；
- 接入功能—资源—角色映射；
- 补充岗位资格规则；
- 提高确定性诊断比例；
- 扩展更多权限问题族。

**交付物：**

- 权威角色目录；
- 功能资源权限映射；
- 权限差异诊断 API；
- 知识审核和更新流程；
- 冲突处理机制；
- 线上评测和回归体系。

---

## 十九、主要风险与控制措施

| 风险 | 影响 | 控制措施 |
| --- | --- | --- |
| 将历史工单经验误当作权威规则 | 错误或越权推荐 | 字段级来源、可信度分级、关键规则必须系统或 Owner 确认 |
| 在线分类使用历史解决方案信息 | 评测虚高、无法真实运行 | 离线分类器与在线理解器分离 |
| 将 L1 当作根因 | 错误推荐角色或维度 | 使用“表象—原因—检查—动作”问题模式 |
| 相似工单直接复用答案 | 忽略用户实时状态 | 强制查询用户权限并执行差异比较 |
| 权限 API 数据延迟或不完整 | 诊断错误 | 返回 freshness 和 limitations，失败时不输出确定结论 |
| 角色目录不完整 | 覆盖率低或误推荐 | 从单问题族开始，逐步核实与扩展 |
| 高风险、跨 BG、涉密问题自动处理 | 安全和合规风险 | 默认转人工或 Owner 确认 |
| 将业务数据异常误判为权限问题 | 无效申请、重复工单 | 保留 S4b 等非权限原因分支 |
| Agent 过度追问 | 用户体验下降 | 定义最低必需上下文和最少追问策略 |
| Agent 强行回答未知问题 | 错误率与安全风险 | 明确停止条件和转人工协议 |

---

## 二十、当前已确认结论

1. 项目目标不是生成供人浏览的权限指南；
2. 四列表格属于聚合报表，不足以支持 Agent 决策；
3. 专题是第一层路由，并存在命中即停；
4. L1/L2 描述用户表象，不描述问题根因；
5. L2 仅适用于 L1=2、3、8；
6. S1～S7 描述历史最终解决结果，不是 Agent 运行时动作；
7. 现有 `classify.py` 适合离线历史分类，不能原样用于在线新工单；
8. 历史工单适合提供用户表达、问题模式、客服经验和已解决案例；
9. 历史工单不能替代角色目录、资格规则和实时权限状态；
10. 同一问题表象可能对应多个候选原因；
11. 权限建议必须区分功能角色与数据维度；
12. Agent 必须结合用户真实权限状态；
13. 确定性的权限差异计算应尽量由后端系统完成；
14. 证据不足时必须追问或转人工，不能猜测；
15. POC 应从一个高频问题族开始历史回放；
16. 首个问题族建议为 `权限专题 + L1=7`，并覆盖全部实际 S 分支；
17. 权限 API 和角色目录的建设范围应由回放中的真实缺口决定；
18. 最终成功标准是降低人工介入并保证解决结果正确、安全、可解释。

---

## 二十一、待决策事项

1. 第一批 POC 最终选取多少案例；
2. 哪些历史工单可确认已真正解决；
3. 权限系统能否直接读取用户角色、有效期和数据维度；
4. 是否存在可导出的角色配置或资源点映射；
5. 角色 Owner 和正式资格规则由谁维护；
6. 用户岗位、部门和 BG 的权威来源；
7. 权限申请入口和生效时间能否系统化获取；
8. 第一阶段只提供建议，还是允许发起低风险申请；
9. 用户确认解决的产品交互方式；
10. 转人工时对接哪个客服或工单流程；
11. 是否能查询缺陷库、DTS 或系统健康状态；
12. 问题模式最终存储介质；
13. 哪些知识需要字段级来源；
14. 知识更新、失效和冲突由谁审核；
15. POC 上线范围、用户范围及灰度策略。

这些问题应通过首个问题族回放和系统调研逐步确认，不宜仅靠架构讨论预先确定。

---

## 二十二、建议的近期行动

1. 按完整专题规则重新确认权限专题数据范围；
2. 统计权限专题下 L1、L2、S 分类和边界分布；
3. 筛选 `权限专题 + L1=7` 的全部案例；
4. 分为基础、歧义和不可自动解决三组；
5. 人工选取 20～50 条代表案例；
6. 标注真实原因、必要追问、最终动作和不应采取的动作；
7. 手工编写第一个“表象—原因—检查—动作”问题模式；
8. 列出该模式所需的全部用户状态和权威规则字段；
9. 调研权限系统、角色目录和组织数据能否提供；
10. 隐藏历史解决方案进行 Agent 回放；
11. 统计失败原因属于知识、数据、工具、规则、推理还是自动化边界；
12. 根据真实缺口决定下一阶段建设优先级。

建议优先级按照以下顺序确定：

```text
用户上下文
→ 功能定位
→ 用户权限状态
→ 角色与维度权威知识
→ 资格与安全规则
→ 问题模式与追问
→ 缺陷或业务数据查询
→ 转人工机制
```

完成首个问题族验证后，再决定是否扩展到：

- L1=2b：如何申请权限；
- L1=3a：无权限报错；
- L1=3b：按钮灰色或不可操作；
- L1=3c：组织、维度或选项不可选；
- L1=6：帮助维护配置；
- L1=8a：权限规则咨询；
- 其他高频且边界清晰的问题族。

---

## 附录 A：POC 交付清单

| 类别 | 交付物 |
| --- | --- |
| 范围 | POC 问题族、用户范围、非目标 |
| 数据 | 代表工单、分层样本、端到端金标准 |
| 分类 | 在线专题路由、L1/L2 理解规范 |
| 知识 | 问题模式、术语库、角色目录 V0.1 |
| 工具 | 用户权限查询、功能权限查询、可选差异诊断 |
| Agent | 对话状态、追问、诊断、解释、转人工 |
| 安全 | 资格校验、可信度、停止条件、高风险路由 |
| 评测 | 离线分类、在线理解、端到端诊断、安全指标 |
| 运营 | 日志、反馈、知识更新、指标看板 |
| 决策 | 是否扩大覆盖、优先补齐哪些能力 |

---

## 附录 B：方案核心关系

```text
历史三维分类
→ 理解工单分布并发现候选问题模式
→ 人工或系统识别真实原因分支
→ 建设问题模式、角色事实和查询能力
→ Agent 在线重新理解新问题
→ 追问并查询真实状态
→ 诊断后决定解决动作
→ 问题结束后记录实际 S 分类
```

其中：

- **专题**负责第一层路由；
- **L1/L2**负责描述用户表象；
- **S 分类**负责描述历史最终解决方式；
- **边界字段**负责标识分类歧义；
- **候选原因、验证条件和运行时动作**属于 Agent 诊断设计；
- **角色目录和权限 API**提供权威事实；
- **Agent**负责理解、追问、编排、解释和转人工；
- **确定性系统**负责权限事实和差异计算。
