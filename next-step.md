你现在需要先把“这个小代码仓库”和“最终要建设的系统”分开理解。

> **当前代码不是最终的权限客服 Agent，也不是知识库平台。**
> 它是一台“可执行的假设验证器”：用最小场景证明，用户的自然语言问题能否被转换成一组结构化事实，再由权威规则和实时权限状态得出安全、可解释的诊断结论。

你原方案里真正想验证的，是“工单经验、权威权限规则、用户实时状态、确定性判断和 LLM 分工是否成立”。当前代码只是把这条链路第一次闭合。

---

# 一、我对最终系统的完整理解

最终要建设的系统，不应被理解为“一个会回答权限问题的聊天机器人”，而应该理解为：

> **一个面向客服场景的权限诊断与决策系统。**

它接收用户的自然语言问题，理解用户想访问什么资源、执行什么动作、查看什么数据范围，然后查询真实权限事实、加载对应访问规则，给出确定性诊断；无法确定时，带着完整证据转人工。

完整系统可以分成七层。

```text
┌─────────────────────────────────────────────┐
│ 1. 用户与客服入口                           │
│ Chat、客服工作台、工单、页面内助手           │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│ 2. 问题理解与上下文收集                     │
│ 页面、业务对象、用户动作、BG、代表处、现象   │
│ 必要追问、歧义消解                           │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│ 3. 能力与策略定位                           │
│ 用户语言 → 标准功能/资源/动作                │
│ 例：searchRevenue + QUERY → 某访问策略        │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│ 4. 权威事实层                               │
│ 用户真实角色、有效期、数据维度、申请状态     │
│ 功能 ID、角色 ID、代表处代码、风险属性       │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│ 5. 确定性诊断与安全决策                     │
│ 缺角色、缺维度、已过期、待生效、权限已满足   │
│ UNKNOWN、涉密、跨 BG、工具失败 → 转人工      │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│ 6. 解决动作与客服表达                       │
│ 申请角色、补数据范围、续期、等待、转人工     │
│ LLM 负责解释，不负责权限计算                 │
└──────────────────────┬──────────────────────┘
                       ↓
┌─────────────────────────────────────────────┐
│ 7. 审计、评测和知识治理                     │
│ 决策轨迹、策略版本、权限快照、用户反馈       │
│ 错误分析、规则更新、回归评测                 │
└─────────────────────────────────────────────┘
```

这七层里，真正构成“系统能力”的不是聊天，而是中间四层：

```text
业务问题结构化
+
权威访问策略
+
用户实时权限事实
+
确定性诊断
```

聊天只是入口和表达层。

---

# 二、当前代码在完整系统中的位置

当前版本只实现了一条最薄的纵向路径：

```text
一句用户问题
→ 规则式语言理解
→ 一个固定访问策略
→ 一份模拟用户权限
→ 确定性诊断
→ 模板化回答
```

它覆盖的是：

```text
RTS
× searchRevenue
× 查询
× EBG
× 代表处数据范围
× 查不到数据
```

所以它不是一个“小型最终产品”，而是完整系统的一条纵向切片。

可以这样理解：

```text
最终系统是一栋楼
当前代码不是一间完整房间
而是从地基、管道、电路到屋顶贯通的一根测试竖井
```

这根竖井的意义是：

* 证明所有层能连起来；
* 找出承重数据模型；
* 提前暴露接口和规则问题；
* 后续扩展时替换各层，而不是推倒重建。

---

# 三、当前版本最关键的部分是什么

## 第一关键：权限领域模型

最关键的不是 FastAPI，也不是规则 NLU，而是下面这三个对象之间的关系：

```text
AccessPolicy
+
EntitlementSnapshot
+
TargetDimension
```

代码位置：

* [模型定义](sandbox:/mnt/data/metaerp-rts-permission-poc/src/metaerp_poc/models.py)
* [访问策略](sandbox:/mnt/data/metaerp-rts-permission-poc/config/policy.rts.search_revenue.ebg.v1.yaml)

它们分别表示：

### `AccessPolicy`

目标功能要求什么权限。

```text
要查询 RTS EBG 收入触发数据：
必须有 EBG Monitor 查询角色
并且该角色下必须包含目标代表处
角色和代表处权限都必须有效
```

### `EntitlementSnapshot`

用户此刻实际拥有什么权限。

```text
用户有哪些角色实例
每个角色当前是什么状态
每个角色下面有哪些代表处范围
这些范围是否有效
数据是什么时间查询到的
查询是否有范围限制
```

### `TargetDimension`

用户本次真正要查看哪个代表处的数据。

```text
Jiangsu Rep Office, CN
```

最终判断，本质上就是：

```text
访问策略要求
-
用户实际权限
=
权限差异
```

---

## 第二关键：角色实例内的数据维度关系

当前最重要的领域判断是：

> **代表处维度必须在目标 RTS 查询角色实例下检查。**

例如用户有：

```text
角色 A：RTS EBG 查询角色
  └── 北欧代表处

角色 B：Billing 查询角色
  └── 江苏代表处
```

用户要查 RTS 江苏代表处数据时，不能把两个角色的信息拼起来：

```text
有 RTS 角色
+
某个其他角色下有江苏代表处
=
有 RTS 江苏权限
```

这是错误的。

正确判断是：

```text
RTS 查询角色实例
└── 是否包含江苏代表处
```

这一承重逻辑位于：

[确定性诊断引擎](sandbox:/mnt/data/metaerp-rts-permission-poc/src/metaerp_poc/diagnosis.py)

对应决策日志中的 `D-002`，也是当前反转成本最高的决策：

[决策日志](sandbox:/mnt/data/metaerp-rts-permission-poc/docs/decision-log.md)

---

## 第三关键：确定性诊断顺序

当前诊断引擎按以下顺序工作：

```text
1. 是否涉及涉密或返回范围受限
2. 权限数据是否足够新
3. 访问策略是否已批准
4. 是否存在所需查询角色
5. 角色是否待生效
6. 角色是否有效
7. 角色实例下是否包含目标代表处
8. 代表处权限是否有效
9. 权限全部满足后停止猜测，转非权限问题
```

对应结果：

| 用户状态          | 诊断结果                                   |
| ------------- | -------------------------------------- |
| 没有查询角色        | `MISSING_ROLE`                         |
| 有查询角色，没有目标代表处 | `MISSING_DATA_SCOPE`                   |
| 查询角色过期        | `EXPIRED_ROLE`                         |
| 代表处权限过期       | `EXPIRED_DATA_SCOPE`                   |
| 权限刚审批，仍在同步窗口  | `PENDING_ACTIVATION`                   |
| 角色和代表处都满足     | `PERMISSION_SATISFIED_BUT_NO_DATA`，转人工 |
| 数据过旧、涉密、策略不可信 | `UNKNOWN`，转人工                          |

这是当前代码的业务核心。

---

## 第四关键：可替换边界

当前版本把容易变化的部分放在接口之后：

```text
NluProvider
PolicyRepository
EntitlementRepository
PermissionDiagnoser
```

对应实现：

[服务编排](sandbox:/mnt/data/metaerp-rts-permission-poc/src/metaerp_poc/service.py)

这意味着未来可以：

* 把规则 NLU 换成 LLM；
* 把 YAML 策略换成策略服务；
* 把 Fixture 权限换成真实权限 API；
* 把本地诊断器换成权限平台的统一 Decision API；

而调用方仍然使用：

```text
POST /v1/diagnose
```

因此，**当前版本最有价值的不是已经写了多少业务逻辑，而是建立了这些替换边界。**

---

## 第五关键：Walking Skeleton 与契约测试

下面这条测试是整个仓库最重要的工程资产之一：

[Walking Skeleton 测试](sandbox:/mnt/data/metaerp-rts-permission-poc/tests/test_walking_skeleton.py)

它始终验证：

```text
用户问题
→ 能力识别
→ 策略加载
→ 权限查询
→ 缺数据维度诊断
→ 正确回答
→ 完整轨迹
```

今后替换真实 API、LLM、存储方式时，这条路径必须保持常绿。

它防止的是：

> 各模块分别开发得很好，但第一次合起来时才发现模型和接口完全不兼容。

---

# 四、当前版本哪些只是占位实现

必须明确区分“骨架”和“临时肉”。

| 当前模块                                      | 性质     | 后续处理                         |
| ----------------------------------------- | ------ | ---------------------------- |
| `PermissionDiagnoser` 核心谓词                | 当前核心   | 保留并逐步扩展，或被权威 Decision API 替换 |
| `AccessPolicy` / `EntitlementSnapshot` 契约 | 承重模型   | 用真实接口验证后修订                   |
| `/v1/diagnose` 请求与响应                      | 初始外部边界 | 原则上只做加法演化                    |
| `RuleBasedNluProvider`                    | PoC 占位 | 后续换 LLM 或混合解析                |
| `FixtureEntitlementRepository`            | PoC 占位 | 后续换真实只读 API                  |
| YAML 单策略文件                                | PoC 占位 | 后续换多策略 Repository 或服务        |
| 模板化客服回答                                   | PoC 占位 | 后续换受约束的 LLM 生成               |
| 本地代表处别名表                                  | PoC 占位 | 后续接标准组织与维度目录                 |
| 本地 POC Role ID / Function ID              | 临时 ID  | 后续映射官方 ID                    |
| 无数据库                                      | PoC 决策 | 进入多策略、在线治理后再引入               |

---

# 五、“三条真实权限接口返回”到底是什么意思

我之前的表达容易让人理解成“需要三条业务工单”或者“三个用户的数据”。

真正想要的是：

> 从真实权限平台取得三种具有代表性的脱敏接口返回，用来验证当前内部权限模型能不能承接真实系统数据。

当前代码假设真实权限数据长这样：

```text
用户
└── 角色实例
    ├── 角色状态和有效期
    └── 代表处数据维度
        ├── 维度代码
        ├── 状态
        └── 有效期
```

但这只是一个尚未验证的假设。

所以希望拿三类真实返回：

## 样例一：有角色且有目标维度

用来确认正常允许访问的数据结构。

```text
用户有 EBG 查询角色
该角色下有江苏代表处
两者都有效
```

期望诊断：

```text
PERMISSION_SATISFIED_BUT_NO_DATA
```

不是因为系统确认有业务数据，而是确认“不再是权限问题”。

## 样例二：有角色但缺目标维度

用来确认代表处范围是否真的挂在角色实例下面，以及缺维度如何表达。

```text
用户有 EBG 查询角色
该角色下只有北欧代表处
没有江苏代表处
```

期望诊断：

```text
MISSING_DATA_SCOPE
```

## 样例三：缺角色或角色过期

用来确认：

* 完全没有角色时 API 如何表达；
* 角色过期时是状态字段、有效期，还是根本不返回；
* 同一用户是否可能有多个相同 Role ID 的授权实例。

期望诊断：

```text
MISSING_ROLE
```

或：

```text
EXPIRED_ROLE
```

这三条不是为了凑评测集，而是为了验证两个高爆炸半径假设：

* `A-003`：维度是否绑定在角色实例下；
* `A-005`：真实 API 是否能映射当前承重字段。

详见：

[假设台账](sandbox:/mnt/data/metaerp-rts-permission-poc/docs/assumption-ledger.md)

如果真实接口不是嵌套结构，也没有关系。例如它可能返回扁平行：

```text
user_id | role_id | scope_code | role_status | scope_status
```

Adapter 可以将它组装成内部模型。

真正危险的是，如果真实系统的权限关系是：

```text
用户角色授权
和
用户数据范围授权
完全独立
```

那么当前 `D-002` 需要重审，诊断数据模型也要改变。这正是为什么要尽早拿真实返回。

---

# 六、系统从“点”演化到“面”的五条轴

不要只把演进理解成“增加更多功能”。系统会沿五个维度扩张。

## 1. 业务范围扩张

```text
一个功能、一个 BG
→ 同一功能多个 BG
→ RTS 多个动作
→ 应收多个子系统
→ MetaERP 多子产品
```

## 2. 诊断深度扩张

```text
缺角色 / 缺维度
→ 过期 / 待生效
→ 角色组合
→ 多级维度继承
→ 资格与风险校验
→ 非权限问题分流
```

## 3. 权威事实接入

```text
Fixture
→ 真实权限查询 API
→ 功能/资源目录
→ 角色目录
→ 组织维度目录
→ 统一权限 Decision API
```

## 4. 自动化等级扩张

```text
只给建议
→ 生成申请参数
→ 帮用户预填申请
→ 用户确认后提交
→ 低风险场景自动执行
```

每次提高自动化级别，都需要重新评估安全和责任边界。

## 5. 知识治理能力扩张

```text
单个 YAML
→ 多策略版本
→ Owner 审核和发布
→ 变更触发回归
→ 在线策略服务
→ 企业级权限本体或能力目录
```

因此，小切入点并不会限制“面”的形成。它是在同时验证这五条轴能否共享一套基本协议。

---

# 七、建议的 Milestone 演进路线

## M0：Walking Skeleton——已经完成

### 要证明

整条链路能闭合：

```text
用户表达
→ 权限事实
→ 策略
→ 诊断
→ 回答
→ 轨迹
```

### 当前成果

* Fixture；
* 单一策略；
* 规则 NLU；
* 确定性诊断；
* API 契约；
* CI；
* 固定评测。

### 这个阶段不证明

* 真实 API 可用；
* 真实语言理解准确；
* 业务规则已由 Owner 签字；
* 用户能真正解决问题。

---

## M1：真实事实接入

### 目标

证明当前数据模型能承载真实权限平台。

### 主要工作

1. 接入真实只读权限 API；
2. 接入真实 Role ID；
3. 接入正式 Function/Resource ID；
4. 接入标准代表处代码；
5. 确认数据新鲜度 SLA；
6. 确认 Pending 和过期语义；
7. Owner 审核并批准 Policy。

### 这阶段最关键的决策点

#### 决策一：维度是不是角色实例的一部分

```text
是
→ 保留当前模型

否
→ 重构为独立授权关系或权限关系图
```

#### 决策二：使用本地诊断还是上游统一 Decision API

如果权限平台能直接返回：

```json
{
  "decision": "DENY",
  "reason": "MISSING_DATA_SCOPE"
}
```

则本系统不应重复实现复杂权限计算，只需适配其结果。

#### 决策三：新鲜度和一致性

* 实时查询还是缓存？
* 多久算过期？
* 查询失败是否一律转人工？
* 权限刚审批后的传播窗口如何判断？

### M1 退出条件

```text
真实 API Adapter 通过契约测试
+
至少三类真实授权形态可正确映射
+
策略有明确 Owner 和官方 ID
+
不修改公共诊断结果语义
```

---

## M2：Shadow 诊断

### 目标

让系统处理真实问题，但不直接回答用户。

运行方式：

```text
真实用户工单
→ Agent 静默诊断
→ 人工客服正常处理
→ 对比两者结论
```

### 主要工作

* 收集真实自然语言；
* 保存当时权限快照；
* Agent 和人工分别给原因代码；
* 分析错误来自哪一层；
* 迭代术语、追问和规则。

### 决策点

#### NLU 采用哪种方式

可能选项：

```text
规则
LLM
规则 + LLM
页面上下文优先 + LLM 补充
```

这里不应提前决定。根据 Shadow 错误数据选择。

#### 什么时候追问

例如：

* 用户没提供代表处时是否追问？
* 能否根据合同号查询合同所属 BG 和代表处？
* 页面上下文能否自动提供 Function ID？
* 需要问用户的信息能否通过系统查到？

#### 原因代码是否需要扩展

真实数据可能出现：

* 多个角色实例；
* 部分范围继承；
* 申请待审批而不是待同步；
* 权限冻结；
* 组织关系变化；
* 角色存在但资格不满足。

这些不能硬塞进已有原因代码。

### M2 退出条件

```text
至少 20～30 条真实 Shadow 案例
原因代码与人工一致率 ≥ 90%
必须转人工召回率 = 100%
越权建议 = 0
```

---

## M3：客服辅助模式

### 目标

先服务人工客服，而不是直接面向用户。

系统为客服显示：

```text
识别到的功能和目标范围
用户真实权限状态
逐项检查结果
推荐动作
证据和策略版本
转人工或升级原因
```

客服确认后再发给用户。

### 为什么先做客服辅助

* 风险低；
* 可以快速获得纠错反馈；
* 客服可以发现规则遗漏；
* 便于建立高质量金标准；
* 不需要立即解决复杂对话体验。

### 决策点

* 客服可以修改哪些字段？
* 客服修改是否回写为训练或评测数据？
* 是否必须选择“Agent 对/错”及错误原因？
* 策略冲突交给哪个 Owner？
* 哪些诊断允许一键采用？

### M3 退出条件

```text
人工平均诊断时长显著下降
重复追问下降
客服采用率达到目标
错误建议与安全事件为零
```

---

## M4：受控用户自助

### 目标

将已经验证过的低风险分支直接回答用户。

建议只开放：

* `MISSING_ROLE`
* `MISSING_DATA_SCOPE`
* `EXPIRED_ROLE`
* `EXPIRED_DATA_SCOPE`
* `PENDING_ACTIVATION`

权限满足但无数据仍转人工。

### 决策点

* 哪些结论需要用户身份二次验证？
* 是否显示用户已有角色和范围？
* 权限信息是否属于敏感数据？
* 回答中可以展示到什么粒度？
* 用户执行后如何验证是否解决？
* 失败一次后是否允许再次诊断？

### M4 退出条件

```text
用户确认解决率达到门槛
重复开单不恶化
安全事件为零
工具失败时确定性回答为零
```

---

## M5：复制到 RTS 其他切片

这时扩展不再是复制整个系统，只是新增：

```text
Capability
+
AccessPolicy
+
实体映射
+
评测案例
```

推荐顺序：

```text
1. searchRevenue × CNBG × QUERY
2. revenueWorkbench 中清晰的单一查询能力
3. Document Archiving
4. Submit
5. Attachment Download
```

每个新能力都要回答：

* 资源是什么？
* 动作是什么？
* 上下文是什么？
* 需要什么角色表达式？
* 需要什么数据维度？
* 有什么强制转人工边界？
* 如何验证成功？

### 关键架构决策

策略从一个 YAML 变成多个后，需要决定：

```text
文件 Repository
→ 数据库
→ 配置平台
→ 专门 Policy Service
```

决定时机不是“文件看起来不专业”，而是出现：

* 多 Owner 并发修改；
* 在线更新；
* 权限审批；
* 版本发布；
* 回滚；
* 多环境；
* 多租户隔离。

---

## M6：扩展到 MetaERP 多子产品

到这一阶段，系统会形成三类共享资产。

### 共享业务语义

```text
页面、按钮、业务对象、用户常见表达
```

### 共享权限语义

```text
角色、数据维度、有效期、资格、风险、申请入口
```

### 共享诊断协议

```text
Capability
+
Action
+
Target Context
+
Policy
+
Entitlement
→ Diagnosis
```

此时新增 AP、CFS、iJournal，不应重新做一个 Agent，而是增加新的 Capability 和策略适配。

---

## M7：从“建议”演化到“行动”

最后才考虑：

```text
生成申请参数
→ 帮用户预填
→ 用户确认
→ 提交申请
→ 查询审批进度
→ 生效后自动验证
```

这是从诊断系统进入行动系统的分水岭。

此时必须增加：

* 权限校验；
* 身份确认；
* 幂等；
* 审批；
* 操作审计；
* 撤销和补偿；
* 高风险人工确认。

这一步不能因为聊天机器人体验好就提前做。

---

# 八、每个 Milestone 的核心产物

| 阶段 | 主要产物                   | 不应投入          |
| -- | ---------------------- | ------------- |
| M0 | 常绿链路、契约、Fixture、规则引擎   | 数据库、知识图谱      |
| M1 | 真实权限 Adapter、正式 Policy | 全量多产品         |
| M2 | Shadow 数据、错误分类、真实评测集   | 直接用户上线        |
| M3 | 客服辅助台、反馈闭环             | 自动申请          |
| M4 | 低风险用户自助                | 高风险权限         |
| M5 | 多 Capability 策略体系      | 企业级大一统本体      |
| M6 | 多子产品共享协议与治理            | 每个产品重复建 Agent |
| M7 | 申请与执行闭环                | 无审批自动授权       |

---

# 九、演化过程中的核心决策点

可以将决策点分成五类。

## 1. 领域模型决策

这些是单向门，要谨慎。

* 数据维度绑定角色实例还是用户？
* 一个 Capability 是否可能对应多个资源？
* 一个策略是否允许角色组合？
* 维度是否有继承关系？
* 多个相同角色实例如何表达？
* 过期、撤销、冻结、待审批是否是不同状态？
* BG 是角色属性、目标数据属性，还是独立约束？

## 2. 权威来源决策

* 哪个系统是 Role ID 的真相源？
* 哪个系统是代表处代码的真相源？
* 谁批准功能到角色的映射？
* 工单、知识案例和配置冲突时信谁？
* 策略多久需要复核？
* 上游变更如何通知本系统？

## 3. 诊断责任边界

* 哪些判断在本系统做？
* 哪些判断交给权限平台？
* 哪些判断必须人工裁量？
* 权限满足后要不要继续诊断业务数据？
* 查询条件问题是否进入自动范围？
* 多原因并存时输出一个还是多个原因？

## 4. LLM 使用边界

* 只做自然语言抽取，还是也做策略匹配？
* 什么时候由页面上下文直接定位，不调用 LLM？
* LLM 输出必须满足什么 Schema？
* 置信度低时追问还是转人工？
* 回答生成是否使用模板、LLM，还是混合？
* Prompt、模型和评测集如何版本化？

## 5. 产品与风险决策

* 先面向客服还是用户？
* 哪些原因允许确定性回答？
* 是否向用户展示具体角色和已有权限？
* 什么情况下必须转人工？
* 自动闭环率和安全性如何权衡？
* 什么时候允许自动提交权限申请？

---

# 十、你作为项目负责人现在应怎样理解代码

不需要先理解每一行 Python。建议先掌握四个问题。

## 1. 系统到底在比较什么

```text
用户想做什么
+
目标数据是什么
+
访问策略要求什么
+
用户实际拥有什么
```

## 2. 哪个结论由谁负责

| 结论         | 责任方         |
| ---------- | ----------- |
| 用户说的是哪个功能  | NLU / 页面上下文 |
| 用户想做查询还是下载 | NLU / 页面上下文 |
| 需要哪个角色和维度  | 权威 Policy   |
| 用户实际有哪些权限  | 权限平台        |
| 缺角色还是缺维度   | 确定性诊断       |
| 怎么向用户解释    | 模板或 LLM     |
| 是否允许申请     | 资格规则或人工     |
| 是否转人工      | 安全规则        |

## 3. 当前代码中最不能随意改的是什么

* `EntitlementSnapshot` 的承重语义；
* 角色实例和数据维度的关系；
* `Diagnosis.cause_code` 的含义；
* 工具失败时停止猜测；
* Policy 版本和证据追踪；
* `/v1/diagnose` 的外部契约。

## 4. 最容易替换的是什么

* NLU 实现；
* 提示词；
* 回答文案；
* Fixture；
* YAML 存储；
* 页面别名；
* 代表处别名。

---

# 十一、接下来建议的实际顺序

目前不要继续扩功能。下一阶段应当是 **M1：验证真实事实接口**。

具体顺序：

```text
1. 权限平台解释真实返回结构
2. 确认角色与维度的真实关系
3. 确认 Role ID / Function ID / 代表处代码
4. 编写真实 API Adapter
5. 用真实数据跑现有全部契约测试
6. 出现结构不一致时，先更新假设与决策日志
7. 保持 Walking Skeleton 常绿
8. 开始 Shadow 收集
```

当前代码仓库的任务不是继续长大，而是作为判断标准：

> 当真实世界的数据进来时，这套抽象是否仍然成立？

相关文件：

* [完整 PoC 仓库]metaerp-rts-permission-poc
* [决策日志]docs/decision-log.md
* [假设台账]docs/assumption-ledger.md
* [待证明命题与退出标准]docs/poc-proof.md
* [确定性诊断核心]src/metaerp_poc/diagnosis.py

最重要的对齐结论是：

> **当前切入点虽小，但我们验证的不是“RTS 江苏代表处怎么查”，而是一套可复制的权限诊断协议：资源与动作定位、权威策略、实时授权事实、确定性差异判断、安全降级和证据追踪。**
> 一旦这个协议被真实数据和真实案例验证，向 CNBG、其他 RTS 动作、其他子产品扩展，主要变成增加 Capability、Policy 和评测案例，而不是重新建设一套 Agent。
